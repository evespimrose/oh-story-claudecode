# 변경 이력

이 프로젝트의 모든 주요 변경 사항은 이 파일에 기록됩니다.

## v0.7.5

> 이 버전은 새 기능 없이 문제 수정과 오버헤드 감소에 집중했습니다. 가장 중요한 점: Claude Code에서 正文을 쓸 때 追踪 상태를 검사하지 않았는데, 다른 세 단말은 v0.7.3부터 검사해서 같은 프로젝트가 Claude Code에서만 追踪 없는 장을 계속 쓸 수 있었습니다. 이번 버전에서 이 검사를 보강했으며, **대가로 기존 追踪의 장편 프로젝트가 Claude Code에서 차단되기 시작하므로 先 `追踪/` 를 마이그레이션한 후 계속 써야 합니다**. 처리 방법은 아래 업그레이드 안내를 참고하세요. 또한 장편 작성 시 매번 트리거될 때마다 통째로 로드되던 SKILL.md를 699행에서 455행으로 줄였으며, 책을 시작할 때만 쓰이는 세 단계를 필요할 때 읽도록 옮겼습니다. 正文 규칙 중 평범한「그가 말했다」를 위반으로 판정하던 규칙이 나머지 11곳의 기준과 충돌했는데, 이 역시 함께 수정했습니다. **본 버전의 `agents_version`은 24**입니다(v0.7.4는 23). 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어야 합니다.

### 업그레이드 안내(이번 버전 필독)

```bash
npx skills add worldwonderer/oh-story-claudecode -y -g
```

그다음 작성 프로젝트 루트에서 `/story-setup`을 다시 실행하고, **새 세션을 여십시오**(전문 에이전트(agent)는 세션 시작 시에만 등록됩니다). 자세한 설명은 `skills/story-setup/UPGRADING.md`를 참고하세요.

**기존 追踪의 장편 프로젝트가 Claude Code에서 차단되기 시작합니다.** `追踪/` 아래에 `_tracking-state.json`이 없는 프로젝트는 v0.7.2 이하의 追踪 구조에 머물러 있으며, v0.7.3부터 마이그레이션이 필요했습니다. 이전까지 Claude Code에서는 평소처럼 쓸 수 있었지만, 이번 버전부터는 쓰기 전에 차단됩니다. 차단 안내에 마이그레이션 방법이 포함되어 있습니다: `/story-import`의「기존 追踪 프로젝트 마이그레이션」을 통해 `追踪/`만 재구성하면 되며, **전체 작품 분석을 다시 돌릴 필요는 없고** 正文, `设定/`, `大纲/`는 모두 영향을 받지 않습니다.

### 수정

- **Claude Code에서 正文 작성 시 追踪 검사 복원(#305)**. 같은 책, 같은 正文 작성 작업이 OpenCode, ZCode, Codex에서는 차단되고 수정 방법을 알려주는 반면, Claude Code에서는 그냥 써져 버렸습니다——追踪 상태 누락, 상태 카드와 실제 진행 불일치, 장 건너뛰기 작성 등을 전혀 막지 못했습니다. 사용자는 종종 追踪 없는 正文을 여러 장이나 써버린 뒤 세션 시작 알림이나 배치 검증에서야 발견했고, 그때는 다시 가져오는 방법밖에 없었습니다. 원인은 v0.7.3에서 추가된 네 가지 追踪 검사가 공유 JS 코어와 Codex의 Python 구현에만 들어갔고, Claude Code에 붙은 별도의 순수 bash 가드에는 하나도 적용되지 않았기 때문입니다. 이제 bash 가드가 같은 공유 코어를 호출하도록 수정해 네 단말의 판정과 안내가 일치하며, 이어쓰기와 수정 작업도 판정 대상에 포함됩니다. 이 검사는 JSON을 파싱해야 해서 node에만 의존할 수 있으며, 기계에 node가 없을 때는 적용되지 않습니다. 细纲 쪽 검사는 여전히 순수 bash라 어떤 상황에서도 막아낼 수 있습니다.

- **正文中의 평범한「그가 말했다」가 더 이상 위반으로 판정되지 않음(#312)**. `story-format.md`에서「그가 말했다」「그녀가 말했다」를 절대 금지로 분류했는데, 전체 저장소에서 평범한「말했다」를 위반으로 판정한 유일한 곳이었습니다. 이는 `format-and-structure.md` 등 11곳의「평범한 말하기는 유지 가능」한 서술과 충돌했으며, 로딩 경로가 마침 `正文/`라 正文 작성 시 반드시 적용되었습니다. 이제「대화 태그의 기계화 피하기」로 변경합니다: 고빈도·정형화된 태그는 동작이나 문맥으로 대체하고, 평범한「말했다」는 저빈도 사용 시 유지할 수 있습니다.

- **正文에 걸리지 않는「AI 어조 금지」레드라인 블록 삭제(#312)**. `story-narrative.md`의 이 블록은 `작품 분석库/`, `대상/벤치마크/`, `设定/` 아래에서만 로드되고 正文 디렉터리는 전혀 걸리지 않았습니다. 또한 다섯 규칙도 narrative-writer의 7 Gate, 금지 사항, 확정성 검사 스크립트에 의해 이미 커버되고 있었습니다.

- **AI스러움 제거용 자연 텍스트 기준이 전보체를 가르치지 않도록 수정(#312)**. story-deslop의 이 기준표는 3개월 전 기준에 머물러 있었고, 여전히「문단은 1-3문장 위주」라고 적혀 있었습니다. 이는 코퍼스 보정보다 앞선 내용으로, AI스러움 제거 스킬(skill) 스스로 글을 전보체로 밀어붙이는 결과를 낳았습니다. 코퍼스 보정에 따라 재작성했고, 문장 내 리듬 행을 보강했습니다.

### 개선

- **장편 작성 매 트리거 시 고정 로딩량 3분의 1 감소(#269)**. `story-long-write`의 SKILL.md는 이 스킬(skill)에서 유일하게 무조건 통째로 컨텍스트에 들어가는 파일이었고, `references/` 아래 수십 개 파일은「참고 자료 색인」을 통해 필요할 때 읽었습니다. 이전 버전 699행 중, 책 시작 3단계(주제 방향 확인, 핵심 설정, 大纲 구축)가 244행을 차지했습니다——이 세 단락은 책당 한 번만 돌아가고 일일 연재나 단장 작성에는 쓰이지 않는데, 매번 트리거될 때마다 로드되어야 했습니다. 이제 `references/workflow-setup.md`로 옮겨 책을 시작할 때만 읽도록 해, SKILL.md가 455행으로 줄었습니다. 남은 부분은 매 장마다 쓰이는 부분: 장면 판단과 정박 규칙, 단장 작성 흐름, 품질 검사, 참고 자료 색인입니다. 이동 전후를 바이트 단위로 비교 확인했으며, 단장 작성과 품질 검사 두 절은 한 글자도 움직이지 않았습니다.

- **narrative-writer 템플릿 약 20% 간소화(#312)**. 매 장마다 한 번씩 스폰(spawn)되고 통째로 상주하는 正文 에이전트(agent) 템플릿이 36506바이트에서 29506바이트로 줄었습니다. 삭제한 내용은 7 Gate, 금지 사항과 중복된 심사 목록(`/story-review`에서 스폰할 때 원래 완전한 채점 기준을 인라인합니다), 正文 단계의「구체적 글자 수 표현 검증」(심사 측으로 이관), 그리고 줄임표·대시·문단 간 공백·장 메타 정보 등의 중복 당부——이들은 매 장마다 정규화 스크립트가 자동으로 처리합니다. 작성 기준이 완화된 것은 아니며, Gate A~G와 금지 사항은 그대로입니다.

### 유지보수

- 正文 작성 가드의 bash 측을 크로스 플랫폼 일치성 단언에 포함시켰습니다. 이전에는 Codex의 Python과 JS 코어만 잠갔고 bash 측에는 아무 단언도 없었는데, 위에 적은 네 단말 불일치가 바로 이 틈새로 빠져나갔습니다. 새로 추가된 단언은「같은 프로젝트, 같은 쓰기 작업에서 양쪽이 막아야 할지 말아야 할지 반드시 일치」를 기준으로 열 그룹의 프로젝트 상태를 비교하며, 막아야 할지 통과시켜야 할지 하나하나 명기했습니다.

## v0.7.4

> 이 버전은 전적으로 문제 수정이며 새 기능은 없습니다. 사용에 가장 큰 영향을 미치는 세 가지: 자신의 책을 가져온 뒤「대상/벤치마크」디렉터리에 자신의 설정이 들어가는 문제; 다중 단말 배포 프로젝트가 세션을 열 때마다 참고 자료 패키지가 없다고 알리는 문제; 작품 분석이 문풍 통계 단계에서 Windows에서 바로 오류로 종료되는 문제. 또한 spawn의 버전 검사를 하드 게이트에서 알림으로 변경해, 버전이 맞지 않아도 전체적으로 싱글 스레드로 되돌아가지 않게 했습니다. **본 버전의 `agents_version`은 23**입니다(v0.7.3은 22). 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어야 합니다.

### 수정

- **가져온 책이 자신의 대상/벤치마크로 등록되지 않게 수정(#294)**. `story-import`가 이전에는「가져온 책」과「대상/벤치마크 책」을 모두 `{책이름}`으로 적어서, 자기 책의 작품 분석 결과가 따라서 `대상/벤치마크/`로 복사되었습니다. 사용자가 보기에는「대상/벤치마크 내용이 자기 설정과 똑같음」상황이었습니다. 이제 둘은 완전히 분리됩니다: 자기 책 작품 분석은 오직 正文, 设定, 大纲, 追踪 재구성에만 쓰이고, 명시적으로 지정된 외부 작품만 `대상/벤치마크/`에 들어가며, 지정하지 않으면 이 디렉터리를 만들지 않습니다. 작성과 책 간 검색도 역사적으로 잘못 만들어진 `대상/벤치마크/{현재 책이름}/`을 건너뛰어, 기존 프로젝트가 계속 오염되지 않게 합니다. 단편에서의 자기 책 분석은「자기 책 연재 기준선」으로 이름을 바꿉니다.

- **버전 불일치는 알림만 하고, 다중 에이전트(agent)를 더 이상 차단하지 않음(#294)**. 예전에는 `agents_version`이 현재 패키지와 일치하지 않으면 전체적으로 solo로 되돌아갔습니다. 하지만 bump는 다른 배포물이 바뀌었을 뿐 에이전트(agent) 템플릿은 전혀 안 움직인 경우가 많아, 이 때문에 병렬성을 희생하는 것은 비용이 맞지 않았습니다. 이제 평소처럼 spawn하고 보고서에 버전 불일치를 언급하며 `/story-setup` 재실행을 권장하는 정도로 합니다. 정말로 solo로 되돌아가는 경우는 에이전트(agent) 파일이 실제로 누락되었거나, 런타임이 사용자 정의 에이전트(agent)를 지원하지 않는 두 가지 상황뿐입니다.

- **장편 글자 수는 오직 细纲 목표만 인정(#295)**. 이전에는 정적 하한 표(장당 2000/3000자)와 훅(hook)의「细纲 목표 × 90%」가 공존해, 두 가지 기준이 적용되었습니다. 정적 표를 삭제하고 细纲 목표 하나만 남깁니다; 细纲에 글자 수 목표를 쓰지 않았을 때는 장당 3000자로 보충하며, 동시에 细纲 보충을 안내합니다.

- **browser-cdp가 Chrome을 일괄 종료하도록 가르치지 않게 수정(#295)**. 기존의 정리 명령은 실행 파일 이름으로 프로세스를 죽여서, 사용자 스스로 열어둔 브라우저까지 함께 꺼버렸습니다. 디버그 프로필에 속하는 PID를 먼저 찾은 뒤 종료하도록 바꿨으며, macOS／Linux와 Windows에 각각 바로 실행 가능한 명령을 하나씩 제시했습니다.

- **흑암(黑岩) 순위 스캔 시 같은 날 다른 채널이 서로 덮어쓰지 않게 수정(#295)**. male, female, all 세 채널이 당일에 같은 파일 이름을 공유해서, 두 번째를 돌리면 첫 번째가 덮여 쓰였습니다. 파일 이름에 채널을 추가하고, 잘못된 `--channel`은 바로 오류로 종료합니다.

- **재배포 시 플랫폼을 잘못 인식하지 않게 수정(#297)**. story-setup이 OpenClaw를 판단하는 기준 중 하나가 스킬(skill)에 `metadata.openclaw`가 붙어 있는지 여부였는데, 13개 스킬(skill) 모두 이 값을 가지고 있었습니다. 또한 OpenClaw, Reasonix, 범용 세 가지 경로로 배포된 `skills/`는 모습이 똑같았습니다. 즉, 자기가 배포한 것을 판단 근거로 삼아 Reasonix와 범용 프로젝트를 모두 OpenClaw로 인식했던 것입니다. Reasonix 쪽은 정반대로: 판단 기준이 찾는 몇몇 파일이 그 배포에서는 생성되지도 않아, 이미 설치된 프로젝트를 오히려 인식하지 못했습니다. 이제 세 단말은 각자 `AGENTS.md`의 제목행으로 구분하는데, 이것만이 서로 겹치지 않는 표시입니다. 이미 배포된 프로젝트는 `.story-deployed`에 적힌 `target_cli`를 우선 읽어서, 매번 다시 추측하지 않게 합니다.

- **다중 단말 배포 시 세션을 열 때마다 파일 부재를 외치지 않게 수정(#299)**. 다중 단말의 `references_dir`는 쉼표로 연결된 여러 경로인데, 세션 시작에서는 하나의 경로로 취급해 찾았습니다. 그래서 제대로 설치된 프로젝트도 매번 자료 패키지 부재를 알렸고, 정말 어느 항목이 부족한지 도리어 알 수 없었습니다. 이제 한 줄씩 확인해서, 정말 부족한 줄만 보고합니다.

- **Windows에서 문풍 통계가 필수적으로 실패하지 않게 수정(#300)**. 이 단계에서는 사용 가능한 Python을 먼저 찾는데, 찾은 것이 Windows 네이티브 버전일 수 있고 샘플 경로는 `/tmp/...`로 적혀 있었습니다. 네이티브 Python은 이것을 `C:\tmp\...`로 취급해서, Git Bash가 파일을 쓴 곳과 달라 당연히 찾을 수 없었습니다. 프로젝트 내 상대 경로로 변경했습니다. 같은 자리에서 이전에는 전체적으로 추가 쓰기를 요구해서, 다시 돌릴 때 지난 라운드의 샘플이 파일에 남아 통계 결과가 두 라운드 섞인 값이 나왔습니다; 이제 첫 번째 단락은 덮어쓰기합니다.

- **목차가 장으로 간주되지 않게 수정(#301)**. 많은 원문 시작에 목차가 붙어 있는데, 거기의「제N장」이 正文中의 것과 마찬가지로 맨 앞에 적혀 있어, 장 경계표를 만들 때 한 장당 두 번씩 세어졌습니다. 이 표는 뒤의 여러 단계에서 유일한 분할 근거라, 한 번 틀리면 계속 틀렸습니다. 이제 행 간격으로 시작의 목차 전체를 걸러냅니다: 목차의 이웃한 두 항목은 한두 행밖에 안 떨어져 있지만, 正文의 장 사이는 한 장 분량이 떨어져 있기 때문입니다. 표에 넣기 전에 장 번호가 이어지는지도 확인합니다. 다권 책에서 각 권이 1장부터 다시 시작하는 것은 정상적인 쓰기 방식이며, 이런 중복은 자동으로 합치지 않습니다.

- **심사의 두 채점 기준 재정렬(#307)**. story-review에는 내장 기준이 하나 있어 파일을 읽지 못할 때 쓰고, 별도로 `quality-rubric.md`가 있습니다. 양쪽이 각자 변형되었습니다: 파일 쪽은「과업 카뎃」이 늘었고, 내장 쪽은「문장 부호 리듬」과「구체적 글자 수 표현 검증」이 늘었으며, 세 가지 차원의 이름도 달랐습니다. 방향도 어색해서, 파일을 읽을 수 있을 때 오히려 두 항목 적은 구판을 썼습니다. 이제 똑같이 18항으로 채우고 단언을 추가해, 다시 갈라지면 CI에서 막히게 했습니다.

### 유지보수

- `tracking_commit.py`의 `context` 필드 설명을 보강했습니다: `init`은 여섯 항목을 받고 `commit`은 네 항목만 받으며, `recent_chapters`와 `next_chapter_commitments`는 커밋 시 도구가 스스로 계산합니다. 예전에는 예제에서 추측해야 했고, `init` 모양대로 `commit`을 쓰면 바로 거절되었습니다(#306).
- OpenCode의 에이전트(agent) 모델을 덮어쓰기 전에 먼저 캐시해야 하는데, 이 문구가 원래 뒤의 절에 적혀 있어 순서대로 실행하면 늦었습니다. 덮어쓰는 단계에 바로 안내를 추가했습니다(#298).
- `format-and-structure.md` 제목이「5가지 절대 금지」인데 밑에는 8가지가 적혀 있었습니다. 세 개의 사본을 함께 고쳤습니다(#306).

## v0.7.3

> 장편 追踪을 단일 권위 트랜잭션 모델로 개편: `追踪/_tracking-state.json`이 유일한 구조화 상태이며, 모든 追踪 쓰기는 `tracking_commit.py`를 거칩니다. 연재 상태 카드와 복선/타임라인/캐릭터 스냅샷은 도구가 통째로 생성하는 파생 뷰가 되었으며, 일일 연재 시 매 장마다 반드시 읽어야 할 파일이 다섯 개에서 세 개로 줄었습니다. Dashboard 디렉터리 트리는 필요할 때 로드하도록 변경; 장 개요는 서술화로, 원문 인용은 엄선으로 변경했습니다. **v0.7.2 이하의 장편 프로젝트는 반드시 `追踪/`를 먼저 마이그레이션한 후 계속 쓸 수 있습니다**. 아래 업그레이드 안내를 참고하세요. **본 버전의 `agents_version`은 22**입니다(v0.7.2 배포본은 21). 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어야 합니다.

### 업그레이드 안내(장편 연재 중 프로젝트 필독)

`/story-setup`을 다시 실행한 뒤, 각 책에 `追踪/_tracking-state.json`이 있는지 확인하십시오. 없으면 기존 追踪 구조입니다: 일일 연재가 멈추고, OpenCode / ZCode / Codex에서 正文을 쓸 때 훅(hook)에 막힙니다.

마이그레이션에 **전체 작품 분석을 다시 돌릴 필요는 없으며**, 正文, `设定/`, `大纲/`, `작품 분석库/`는 모두 영향을 받지 않고 `追踪/`만 재구성합니다: `/story-import`의「기존 追踪 프로젝트 마이그레이션」을 통해——마지막으로 완성된 장 번호 N을 세고, 기존 追踪 파일과 최근 몇 장 正文으로 현재 상태를 역추적하여 `last_chapter=N`의 초기화 트랜잭션을 만들어 `init`을 한 번 돌리십시오. 기존 구조는 그대로 `追踪/_旧追踪存档/`로 전체 옮겨지며, 삭제하지 않고 파싱에도 참여하지 않습니다. 자세한 설명은 `skills/story-setup/UPGRADING.md`를 참고하세요.

### 신규

- **Dashboard 필요 시 로드 디렉터리 트리(#273)**: 디렉터리 트리를 펼칠 때만 자식 노드를 가져오도록 바꿔, 대형 프로젝트 첫 화면에서 전체 트리를 한꺼번에 스캔하지 않게 했습니다. 덤으로 단편 표준 프로젝트 인식과 지연 로딩 회귀(regression)를 수정했고, 검색 읽기 실패는 침묵하여 삼키는 대신 명시적으로 표시하게 했습니다.

### 수정

- **장편 追踪을 단일 권위 트랜잭션 모델로 개편(#269 #290 #289)**: `追踪/上下文.md`는 고정 7열, 상한 12288바이트의 연재 상태 카드가 되었으며, 매 장마다 도구가 통째로 재구성합니다. 책 전체 역사는 `追踪/逐章记录/第NNN章.md`로 옮겨집니다(일일 연재 시 읽지 않음). 복선, 타임라인 이중 뷰, 캐릭터 스냅샷은 파생 뷰로 격하되며, 검색은 story-explorer를 통해 필요할 때 조회합니다. 일일 연재 시 매 장마다 반드시 읽어야 할 파일이 다섯 개에서 세 개로 줄어들어, 읽기 비용이 O(N²)→O(N)이 되었습니다. `narrative-writer`는 더 이상 `追踪/`에 쓰지 않습니다; `/story-review`에 追踪 파일 유지보수를 추가; SessionStart에 상태 카드 크기 경보를 추가하여 네 단말이 동기화됩니다. 기존 구조와 `_tracking-meta.json`, `时间线/事件库.json`은 함께 폐기되며 호환 계층은 제공하지 않습니다.
- **위 항목이 남긴 메인 흐름 단절 수정**(발표 전 메인 흐름 심사로, 한 줄씩 실측하여 재현):
  - 재가공 백업이 가드에 의해 죽는 문제——가드가「파일 부재」를「새 장 첫 작성」으로 취급해서, `workflow-revision`이 규정한 `正文/第X章_..._原稿_{날짜}.md` 백업이 필연적으로 순서 검사에 걸렸습니다.「제5장을 첫 작성하려면 먼저 제4장 트랜잭션을 제출해야 함」이라고 보고하는데, 재가공 장 번호는 이미 제출된 장 번호보다 항상 작아 답이 없었습니다. 장 번호가 이미 追踪 범위 내에 있을 때는 순서 검사를 건너뛰며, 장 건너뛰기 작성은 여전히 막습니다.
  - 기존 프로젝트에 마이그레이션 경로가 없는 문제——일일 연재에서는 `story-import`로 가라고 하고, `story-import`는 `.active-book`를 보면「다시 돌리지 말고 돌아가서 일일 연재해」라고 답해, 사용자가 두 명령 사이를 왔다 갔다 했습니다. 판단 기준을 `_tracking-state.json` 존재 여부로 바꾸고, `追踪/`만 재구성하는 마이그레이션 단계를 제시합니다; `UPGRADING.md`는 이전까지 전문에서 이번 追踪 변경을 한 번도 언급하지 않았는데, 완전한 절을 보강했습니다.
  - 복구 명령이 수렴하지 않는 문제——「어떤 실패든 같은 commit을 다시 돌리기」는 쓰기 실패에만 성립합니다; 검증 실패는 다시 돌려도 결과가 같고, 파생 뷰가 바뀐 뒤 다시 돌리면 stale에 걸리는데 도구에 rebuild 서브커맨드가 없습니다. 실패 유형별로 따로 적고, `mode=revision`으로 통째로 재구성하는 실측 가능한 출구를 보강했습니다. `check` 실패 시 stderr에만 ERROR를 찍고 JSON을 출력하지 않으므로, 현재 개정 번호는 `_tracking-state.json`의 `state_revision`을 직접 읽도록 바꿨습니다.
  - `story-explorer`와 `consistency-checker`에 `tracking_commit.py check`를 실행하라고 요구했는데, 이 두 읽기 전용 에이전트(agent)는 설계상 Bash를 금지하고 있었습니다. 호출측이 메인 세션에서 `check`를 돌린 뒤 전달한 값을 소비하도록 변경해, 권한 경계는 움직이지 않으면서 장 수에 따라 늘어나는 완전한 state를 프롬프트(prompt)에 읽어들이지도 않게 했습니다.
  - 배포된 `story-narrative` 규칙이 에이전트(agent)에게 `逐章记录/`, `上下文.md`와 복선「정답」을 직접 쓰도록 가르쳐서, 같은 디렉터리의 `story-consistency` 규칙과 정면으로 충돌했습니다. 게다가 设定을 바꾸면 바로 발동했습니다; `伏笔.md`는 파생 뷰라 해당 열이 없는데, 직접 쓰면 `check`에서 불일치로 보고했습니다. 이제 트랜잭션에 등록하도록 변경했습니다.
  - Claude Code의 bash 가드와 JS 코어가 `작품 분석库/` 면제에서 어긋나 있었는데, 동기화해 조였습니다; 미결산 게이트에서「이전 장」을 가져올 때 readdir 원본 순서를 쓰다 보니, 재가공 백업을 통과시킨 뒤 해시 순서 파일 시스템에서 정제되지 않은 백업을 골라버리는 문제가 있었는데, 네 단말이 통일적으로 제외하고 정렬하도록 했습니다.
  - 다섯 플랫폼 AGENTS 템플릿과 세 narrative-writer가 여전히 `追踪/`를「타임라인 이벤트 라이브러리」를 포함하는 것으로 설명했는데, 해당 파일은 도구 폐기 블랙리스트라 만들어내면 commit/check가 영구 실패합니다.
  - 가드 계층: 읽기 전용 에이전트(agent)의 Bash 검출식이「`cmd` 실행」만 알아보고「실행 / 돌리기」는 완전히 우회했습니다——위 항목의 권한 초과 명령이 바로 이렇게 CI 전체를 통과했던 것입니다. 정규식을 보강하고 행 단위로 위임 구문을 면제합니다. 한 단언은 story-import가 기존 追踪 마이그레이션을 설명하는 것을 금지하고 있었는데 본 버전 동작과 반대였으므로,「마이그레이션은 반드시 아카이브 재구성으로 거치기」로 고쳤습니다. 재가공 통과 분기에 parity 회귀(regression) 유즈 케이스를 보강했습니다.
- **장 개요 서술화, 원문 인용 엄선(#275 #276)**: 개요를 더는「…때문에 …하므로」로 연결하도록 요구하지 않고, 시간 순서대로 사건·원인·결과를 명확히 서술하도록 바꿨습니다. 방향을 바꾸는 동작과 결과, 비정상 정보, 장 간 복선 단서를 우선 유지합니다. 원문 인용은 핵심 전환, 핵심 대사, 쓰기 표본만 남기고 장당 최대 8개로 제한합니다——실측 인용이 장 요약의 45.5%를 차지했는데, 다운스트림 Stage 3 사실 추적은 원문으로 돌아가고 Stage 6 문풍 앵커는 원문 슬라이스에서 가져와, 노드별 인용을 전혀 소비하지 않았기 때문입니다. 병렬 chapter-extractor와 직렬 두 경로의 요구 사항을 하나의 템플릿으로 수렴시켰습니다.
- **Dashboard 프로젝트와 작품 분석库 노드 예산 격리(#268)**: 두 종류 디렉터리가 더는 같은 노드 예산을 공유하지 않습니다. 이전에는 초대형 프로젝트를 먼저 스캔해 예산을 소진하면, 작품 분석库가 실제로 있어도 빈 배열을 반환해 인터페이스가「예산이 다른 종류에 소진됨」을「작품 분석库가 비었음」으로 표시했습니다. 이제 각자 스캔 상태와 예산을 가지며, 어느 한쪽이 한계에 다다르면 자기 자신만 잘라냅니다.
- **단편 가져오기와 분량 분기(#285 #286 #287 #288)**: story-import가 단편 분해 파이프라인을 호출할 때 상대방「정박점 없음」이라고 주장하지 않게 했습니다——그 Phase 1에는 글자 수 라우팅과 이어달리기 삼선택 두 개의 질문 지점이 있었습니다. 이제 네 개 Step별로 가져오기 시나리오에 값을 하나하나 주며, 장르 식별은 그대로 돌립니다(`genre_detected`는 차단级 필수 입력). 분량 분기에 글자 수 상한을 보강해, 이전의「장 구분자가 없으면 단편」은 글자 수를 보지 않아 십만자 짜리 벌거숭이 텍스트가 단일 파일 단편 프로젝트로 만들어지곤 했습니다. `选题决策.md`에 자동 발견을 보강했습니다(작성과 작품 분석은 이전까지 모두 프로젝트 루트만 보고, 되채우기 기능은 오랫동안 빈 바퀴만 돌았습니다). story-setup에 참고 디렉터리 자가 진단을 추가해, Windows에서 `npx skills add`가 가끔 일으키는 부분 설치를 검출합니다.

### 유지보수

- 참고 자료 파싱 순서에 `.agents/skills`(Codex / Reasonix가 실제로 스캔하는 skill 루트)를 보강하고, 마지막 층을「현재 런타임이 본 스킬(skill)을 로드한 디렉터리」로 다시 썼습니다; story-setup Phase 2에 배포 멱등성을 명기했습니다(#282).
- README / README_EN에 방식 일의 업그레이드 설명과 Windows 부분 설치의 증상 및 수정 방식을 보강했습니다.

## v0.7.2

> 로컬 워크벤치 `/story dashboard`(127.0.0.1에만 바인드, 프로덕션 의존성 제로); 细纲 형태가 正文에 그대로 베껴져 생기는 경직과 장尾 요약체 치료; 저장소 품질 게이트가 파괴적 실패를 더 이상 덮지 않음; 세션 시작의 두 가지 장기 오보 수정. **본 버전은 `agents_version`을 21로 배포하며, 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어야 합니다**——v0.7.1 배포본은 20이어서 재배포하지 않으면 v0.7.1, v0.7.2의 正文과 훅(hook) 변경이 모두 적용되지 않습니다.

### 신규

- **Story Dashboard 로컬 워크벤치**: `/story dashboard`(Codex는 `$story dashboard`)가 현재
  워크스페이스에서 프로덕션 의존성 제로인 로컬 Web UI를 시작합니다. `작품 분석库/`와 장단편 작성 프로젝트 파일 트리를 따로 보여주며,
  파일명 검색, Markdown 안전 미리보기, 화이트리스트 텍스트 가벼운 편집, 빠른 저장, 확인 삭제와 mtime
  충돌 보호를 지원합니다; 서비스는 기본적으로 `127.0.0.1`에만 바인드되며, 경로 정규화, 실제 경로 검증과 2 MiB 상한으로
  경계 넘기 읽기·쓰기를 막습니다.
- **실제 demo 기반 브라우저 테스트**: 기존《반룡》《증장애의사랑》 작품 분석 자산을
  `demo/작품 분석库/{책이름}/`에 정리하고,《너한테 계정 맡겼다고 너 하이랜드 편집폭발 전국민》프로젝트를
  `demo/장편/{책이름}/`에 정리했습니다; Node API 회귀(regression)와 Playwright 데스크톱/모바일 E2E는 모두 이 기존 demo 묶음을
  임시 디렉터리에 복사한 뒤 실행해, 실제 산출물과 동떨어진 fixture를 따로 만들지 않게 했습니다.

### 수정

- **细纲 형태가 正文에 그대로 베껴져 생기는 경직과 장尾 요약체 치료(#255)**: 细纲의 각 필드는 요약 원시어라, 약한 모델이 1:1로 렌더링해 한 문장 한 단락으로 만들면 전보체가 되고 한비자交代가 됩니다. writing-craft에「细纲에서 正文으로」가드레일을 보강했습니다——细纲은「무슨 일이 일어날지」의 내용 규격이지 正文 형태를 규정하지 않으며, 正文은 플롯 포인트를 합치고 끼워 넣고 재배열해 각 포인트를 장면으로 연출하지 요약어를 그대로 베끼지 않는다는 내용입니다. 시작 부분에 anti-fingerprint를 보강; story-review에 동질화 판단을 추가했습니다. 细纲 템플릿의「结尾 / 结尾设定」을「어떤 상태로 수렴하는지」에서「마지막에 누구의 어떤 동작, 장면, 대사에 걸치는지」로 질문을 바꿨습니다——실제 인간 코퍼스 실측 장편 장尾 대사 마무리 약 29%, 동작 장면 약 26%, 명시적인 상태 요약은 약 1%밖에 되지 않았습니다. 배포 훅(hook)의 독한 문장 미결산 게이트에 `trailer-summary` 규칙을 신규 추가했습니다(문말 600자 윈도우, 네 단말 동기화).
- **저장소 품질 게이트가 파괴적 실패를 더 이상 덮지 않음(#265)**: Dashboard, 훅(hook), 어댑터와 컨트랙트 검증 체인 상의 fail-open 가드, 쓰기 누락 레이스 컨디션, 순위 스캔 부분 실패 보고 누락과 테스트 사각지대를 수정했습니다. Dashboard 저장은 mtime 낙관적 동시성 제어에서 버전 검증으로 바꾸고 직렬화 쓰기 큐에 넣었습니다(같은 초에 병렬 쓰면 이미 승인된 원고를 서로 덮어썼습니다); 순위 스캔 단일 플랫폼 실패는 전체 라운드를 중단하지 않고, 사용 가능한 부분 결과를 유지하며 수집 품질을 사실대로 표시합니다.
- **세션 시작의 두 가지 장기 오보(#173)**: `작품 분석库/`의「미완료 작품 분석」알림을 `_progress.md`의「최종 상태」값으로 필터링하도록 바꿔, `completed` / `completed_with_errors`는 더 이상 집계하지 않습니다——원 구현은 파일을 맨 세어서, 분석이 끝난 책도 매 세션마다 한 번씩 보고했습니다; 값은 콜론 뒤의 상태 자체만 인정하며, 템플릿 플레이스홀더와 괄호 주석 기입은 미완료로 처리해, 적게 보고하는 것보다는 많게 보고하는 쪽으로 합니다. 수동 버전 갱신 알림은 24h 단위로 알림 자체를 스로틀링하도록 바꿨습니다(원 구현은 네트워크 요청만 스로틀링해 같은 버전이라도 세션 열 때마다 한 번씩 알렸습니다). curl 실패는 네거티브 캐시를 써서, GitHub에 닿지 않는 환경에서 매 세션마다 5초씩 허공에서 기다리지 않게 했습니다. `/story` 라우팅 테이블에 `story-review`를 보강했습니다——이전 13개 스킬(skill) 중 유독 심사만 라우팅 행이 없었습니다.

### 유지보수

- README / README_EN을 현재 사용자 경로에 집중시키고, 즈후 옌옌(知乎盐言)의 영어 철자를 수정했습니다(#264、#266).

## v0.7.1

> 正文「전보체」철저한 치료——쓰는 쪽에서 빼기 + story-deslop 저항 제거 + 전체 짧은 문장 숭배 청소(코퍼스 + 오 장르 실측 이중 입증); 동인 / 기존 세계관 명명 가드레일; Reasonix skills-only 어댑트 + 책 시작 시 대상/벤치마크 발견.

### 수정

- **正文 전보체 철저 수정(#256, #254 후속)**: #254에서 쉼표 흐름 문장 기준선을 세워도 완전하지 않았습니다——제보자가 `/story-deslop`을 돌릴 때 모델이 연속된 짧은 문장을「의도된 리듬」으로 취급해 전보체 제거를 거부한다고 지적했습니다. 이번에 삼계층에서 빼기 작업을 진행했습니다:
  - **쓰는 쪽**:「짧은 문장 = 결단력 넘치는 열혈 / 감정 밀도 높음」보상, 시작 부분 이벤트 밀도의 전보체 예제를 삭제(쉼표 흐름으로 교체); narrative-writer「심리 외현 / Gate C / 감정어 기본 외현」을 절대화에서 상한 설정으로 변경(템플릿 + opencode/codex는 sync 스크립트로 재생성, 단편 장르 패키지는 예외로 그대로 유지).
  - **story-deslop 저항 제거**: anti-ai-writing 규칙 3에서「연속 짧은 문장은 세 가지 상황에서만 연속 사용」허가를 삭제하고,「기본은 쉼표 긴 문장 + 짧은 문장은 가끔 쓰이는 고립된 강조」만 남깁니다; 패턴 9 피크 보호는 유지; story-review 채점표 / style-craft / style-profile 등 다운스트림 미러에 모순되게 남아 있던「연속 짧은 문장」「문장 길이 쪼개기」잔여분을 수정했습니다.
  - **전체 짧은 문장 숭배 청소**: 39개 파일 전수 조사, 한 군데씩 판정해서「짧은 문장 위주 / 짧은 문장 다수 채용 / 짧은 문장 비율 >70% / 길면 쪼개기」등 잔여분을 청소했습니다; 예제 문장 after를 전보체에서 쉼표 긴 문장으로 바꾸고, 흑암(黑岩)「한 문장 한 단락」템플릿을 실제 흑암(黑岩) 코퍼스로 교체했으며, 단편에 전 문장 상한을 보강해「옌옌(盐言)이 130자 흐름 문장으로 늘어지는」과잉 교정을 수정했습니다.
  - 증거: mongo 베스트셀러 코퍼스 실측 서술 쉼표 비율 1.2–1.74, 연속 짧은 문장 런은 1.7–4.3%에 불과; codewhale + DeepSeek-V4-Pro 오 장르 실측에서 구 규칙은 모두 전보체로 붕괴되었고 신 규칙은 모두 유창하며 0 전보 런, 전투 피크 / 감정 절제 / 얼탱이 deadpan은 후퇴하지 않았습니다.

### 신규

- **동인 / 기존 세계관 명명 가드레일(#257)**: character-basics 주인공 / 조연 카드, genre-catalog 동인 유파, plot-special-topics 동인 작성 요령에 명명 일관성 제약을 보강했습니다——원작 캐릭터는 공식 본명 / 역명을 그대로 쓰고, 신규 캐릭터는 원작 해당 지역 / 문화의 명명과 역명 스타일에 맞추며, 같은 지역에서 어족을 섞지 않고, 设定과 맞지 않는 현대화 / 정치화 이름을 쓰지 않습니다(设定은 상대적이므로, 현대 设定 동인은 제한받지 않습니다). 저코스트 보충으로 시대착오적 이름 지을 확률을 낮추는 비확정적 수정입니다.
- **Reasonix skills-only 어댑트 + 책 시작 시 대상/벤치마크 발견(#253, Closes #252)**: 신규 `target_cli=reasonix`(skills 복사 + `.agents/skills` symlink + Reasonix AGENTS.md, hooks/agents 없으면 solo/direct로 진행); 책 시작 단계에「대상/벤치마크 발견」을 추가해 `작품 분석库/`를 능동적으로 스캔하고 장르별로 대상/벤치마크 책을 추천하며(장편 Phase 1 / 단편 Phase 2) + 卷纲 알림을 추가합니다.

### 내부 / 코퍼스 거버넌스

- **正文 문장 길이 기준선(#254)**: anti-ai-writing 규칙 3을「문장 길이 기준선(짧음은 도구지 바탕이 아님)」+ 코퍼스 보정 주석으로 재작성했습니다(치마오(七猫) 125권×前 8장: 쉼표 사이 8.8–9.6자, 전 문장 22–24자, 쉼표 흐름이 서술의 74–80% 차지); banned-words에서 슬슬 / 살짝 / 살며시 / 담담히를 2급 밀도 제어로 강등; 각지의「길면 쪼개기 / 기계적 장단 교대」레시피를 제거했습니다. `agents_version`이 **20**으로 올랐습니다.

### 업그레이드

- 이미 배포된 프로젝트는 `/story-setup`을 다시 돌려 references / agents를 갱신하고 **새 세션을 여십시오**; `agents_version` 20에는 #254 + #256의 작성 규칙 변경이 포함되어 있습니다.

## v0.7.0

> 다중 단말 어댑트 추가 2사(ZCode / Reasonix)· 훅(hook) 코어를 공유 node 코어로 통일 + 육단말 parity 락 · 장편「플롯 유닛」개념 통일하고 작품 분석 산출물 연결 · AI스러움 제거 게이트 기계화(독한 문장 확정성 검출 + 미결산 게이트) · 컨트랙트 자가 진단과 스크립트 강화

### 신규

- **ZCode 3.3.4 네이티브 어댑트(#234)**: 신규 `.zcode-plugin/plugin.json`과 루트 `marketplace.json`을 추가해, 저장소를 `oh-story` plugin로 노출시켜 13개 Skills, 13개 Commands와 엄격한 JSON Hooks를 제공합니다; `story-setup`에 신규 `target_cli=zcode`를 추가해 `.zcode/skills` / `.zcode/commands` / `.zcode/hooks`를 배포하고, `.zcode/config.json`과 루트 `AGENTS.md`를 안전하게 병합합니다. 제3자 의존성 없는 Node hook runner가 SessionStart 컨텍스트/연속성 복구, PreToolUse 大纲 가드와 commit advisory, PostToolUse 正文 경량 확정성 망을 커버합니다; 비공백 stdout은 ZCode가 받아들이는 엄격한 JSON만 출력하며, 예외는 stderr에 쓰고 fail-open합니다. ZCode 3.3.4는 프로젝트/plugin custom agents를 실행하지 않고 `.zcode/rules`/PreCompact/SessionEnd가 없으므로, 전문 Agent가 관여하는 Skill과 story-review는 명시적으로 solo/direct로 다운그레이드해 플랫폼 능력을 위조하지 않습니다.
- **Reasonix 네이티브 지원 Phase 1(#238)**: 신규 루트 `reasonix-plugin.json` plugin manifest(version은 `skills/story/VERSION`에 고정)와 README 설치 설명을 추가했습니다; Reasonix는 `.agents/skills`(Codex와 공유하는 `skills/` symlink)를 스캔해 13개 스킬(skill)을 발견하며, `check-reasonix-adapter.sh`가 manifest를 가드합니다. 프로젝트级 `story-setup` 배포와 hooks는 후속 단계에 넘기고, 현재 전문 Agent가 관여하는 흐름은 solo/direct로 진행합니다.
- **단편 장르 스타일 패키지를 플랫폼 코퍼스로 재구축(#231)**: `story-short-write` 장르 스타일 패키지를 4개에서 10개로 확장했습니다(신규: 세정 슬랩스틱, 민속 괴담, 서스펜스, 달콤한 반려, 더블 주인공, 얼탱이 브레인스톰). 치마오(七猫)/즈후(知乎)/흑암(黑岩)/점중(点众) 네 플랫폼 실제 코퍼스로 시작 패턴, 쾌감 포인트 밀도, 대화 스타일, 감정 패턴과 결말 패턴을 재구축하고, 세정 장르 잘못된 라우팅을 수정했습니다.

### 개선

- **장편「플롯 유닛」개념 통일하고 작품 분석 산출물 연결(#246)**:「플롯 바 / 루프 카드 / 정식 플롯 루프 / 플롯 단편」다섯 가지 혼용되던 호칭을 **플롯 유닛**(卷纲 안에서는 **플롯 유닛 카드**로 기재)으로 통일합니다. 필드명 循环ID/循环节拍/… → 单元ID/单元节拍/…로 변경합니다(「循环」은 리듬 의미인 쾌감 포인트 루프 등에만 남깁니다). 작품 분석 플롯 유닛을 卷纲/细纲에 연결합니다: 卷纲 플롯 유닛 카드에「대상/벤치마크 플롯 참조」를 신규 추가하고, 대상/벤치마크 리듬 이관은 플롯 유닛을 선별 단위로 하며, 细纲 분할 경계는「한 묶음 = 한 플롯 유닛」으로 바꿉니다. 작품 분석 쪽 `剧情/README.md`에「플롯 유닛 목록」색인을 신규 추가합니다(기존 책은 기계적으로 보충 가능). 구판 卷纲/细纲/작품 분석库에 이 필드들이 없어도 전혀 막히지 않고 필드 구조로 폴백해서 읽으며, 细纲 보충/수정 시에만 업그레이드됩니다. story-long-write 장면표에「细纲 보충/확장」진입점과 卷纲 잠금 정의를 신규 추가합니다.
- **독자 계약 + 종국 비축 추진 모델(#237)**: 기존「성장 예산」을「독자 계약 + 종국 비축」이중 추진 모델로 대체해, 단장 쾌감을 풀어주고 장편 추진이 너무 빨라 후반에 쓸 게 없어지는 문제를 치료합니다; Σ 글자 수 예산 계약(밀/소 예산, Σ∈[장 목표, ×1.1])은 그대로 둡니다.
- **AI스러움 제거 게이트 기계화(무상태, #246)**: 쓰기 후 正文 망에 확정성 독한 문장 검출을 신규 추가했습니다(「A가 아니라 B」전체 패밀리, 성선 반전, 부정 열거, 예고 결말). 디스크에 떨어지자마자 자동 스캔해 적중 건을 푸시백합니다; 다음 장을 쓰기 전「독한 문장 미결산 게이트」를 신규 추가합니다——이전 장에 미처리 blocking 적중이 있고 `<!-- 去味:跳过 -->` 면제 표시가 없을 때 차단합니다(판정 기준은 현재 파일 자체에서 계산하며, 어떤 상태 파일에도 남기지 않고 node 누락이나 파싱 실패 시 일률적으로 통과시킵니다). 면제 표시는 콜론 전각 반각 모두 인정하며, 동시에 쓰기 후 망이 해당 장 독한 문장 푸시백을 건너뛰게 합니다(나머지 망은 평소대로). `check-ai-patterns.js`에 동기화해 voice-contrast / negation-parade / reverse-not-is / trailer-ending(blocking, 실제 인간 코퍼스 0 오보로 캘리브레이션)과 quote-emphasis-tic(advisory)을 신규 추가했습니다. 네 단말(Claude/OpenCode/Codex/ZCode)이 같은 판정을 공유하며, js↔py 일대일 parity 락 + 따옴표 플레이스홀더 정렬로 check-ai-patterns.js를 고정했습니다.

### 재구성

- **훅(hook) 코어를 공유 node 코어로 통일 + parity 락(#239 / #243)**: OpenCode와 ZCode가 같은 `story_hook_core.js`로 수렴하고(ZCode 훅(hook) 564→198행), Claude의 4개 bash 훅(hook)도 내장 python을 해당 공유 코어로 수렴시켜(`story_hook_cli.js` 경유), 이전까지 유일하게 가드 없고 표류하던 수동 구현을 없앴습니다; Codex는 python 구현을 남기되 py↔js 일대일 parity 락(`test-prose-net-parity.sh` A–E 계층)으로 고정했습니다. node 누락/손상 시 fail-open 회귀(regression)를 보강; 大纲 차단은 순수 bash 보충 가드를 그대로 둡니다.
- **스킬(skill) 컨트랙트 자가 진단 + fail-fast(#242)**: 신규 `check-current-skill-contracts.py` + `current-contract.json`을 추가해, 버전 앵커, Phase, schema, 주요 산출물 경로와 细纲 필수 입력을 CI 컨트랙트로 고정했습니다. `agents_version`이 런타임 만료 판정의 유일한 권위가 됩니다; 대상/벤치마크 주요 산출물(`剧情/情绪模块.md` / `剧情/节奏.md`) 누락은 fail-fast로 바꿔, 더는 기존 산출물로 침묵하며 다운그레이드하지 않습니다.
- **저장소 스크립트 강화(#233)**: 공유 파일/OpenCode 동기화 검사를 읽기 전용 + 쓰기 안전으로 바꾸고, 어댑터 생성기는 원자적 배포 + 예외 롤백을 하며, 네 단말 실제 CLI 설치의 호환 workflow를 신규 추가했습니다.

### 수정

- **Codex 훅(hook)에서 빈 `.active-book` 첫 행을 저장소 루트로 착각하는 문제(#235)**: `.active-book` 첫 행이 비어 있을 때 더는 저장소 루트를 활성 서적으로 오판하지 않으며, bash 오라클과 일치시킵니다.

### 기타

- **배포 검사의 문구 게이트 제거(#240)**:「UPGRADING/README에 반드시 어떤 말을 써야 한다」류의 취약한 문구 게이트를 삭제하고, `agents_version` 임계값 등 행위 앵커는 유지합니다.

### 발표 준비

- 버전 번호를 `0.7.0`으로 올렸습니다(`.claude-plugin/marketplace.json` + 루트 `marketplace.json` + `.zcode-plugin/plugin.json` + `reasonix-plugin.json` + `skills/story/VERSION`). `.story-deployed`의 `agents_version`은 이번 주기에 `17`에서 `19`로 연속 상승했습니다(#242 → 18, #246 → 19). `setup_skill_version`은 `1.2.7`입니다; 본 버전에는 hooks / agent 템플릿 / 프로젝트 규칙 템플릿의 행위 변경이 포함되어 있으므로, 이미 배포된 프로젝트는 `/story-setup`(Codex는 `$story-setup`)을 다시 실행하고 **새 세션을 열어** 적용받아야 합니다. v0.6.22에서 업그레이드하는 경우 한 번 재실행으로 바로 적용됩니다. `UPGRADING.md`에 v18 / v19 항목을 신규 추가했고, `README` / `README_EN` 버전 설명은 최근 3판으로 수렴시켰습니다(더 이전은 CHANGELOG 참고).

## v0.6.22

> 장편 장르 正文 프롬프트 카드 + 단편 투고 층 + 전체 키트 문서 슬리밍(#226 / #227 / #228)

### 신규

- **장르 正文 프롬프트 카드(#226, #222/#223/#224 병합)**: `story-long-write`에 신규 `genre-prose-cards/` 32장 판치에(番茄小说) 장르 억양 카드 + 색인 회수 규범을 추가했습니다; 작성 시 `设定/题材定位.md`에 따라 매칭 회수해 단일 카드를 작가 에이전트에 넘기며, anti-leak 하드 제약으로 카드명/장르 태그/신뢰도/항목/합법성 자가 평가가 일체 正文에 들어가지 않도록 보장합니다; narrative-writer 삼단말 템플릿에 동기화해 회수와 장르별로 세분화한 문풍 핑거프린트/Gate G 규칙을 연결하고, chapter-extractor 템플릿에 `chapter_formula` 장별 쓰기 공식 산출물을 신규 추가했습니다.
- **단편 투고 층(#227)**: `story-short-write`에 신규 `submission-craft.md`를 추가했습니다——즈후 옌션/미니프로그램/판치에(番茄小说) 세 가지 플랫폼 톤 매트릭스(시점, 갈등 진전, 장末 훅(hook), 결말 질감), 서론 정면을 별도로 갈고 닦기(4차원 뼈대+황금 삼각, 150-220자), 유료 포인트 목 브레이크포인트와 역추론법으로 细纲 배열하기; `story-short-analyze` 분해 시 투고 층을 덤으로 기록해 작품 분석 리포트에 넣습니다. 병합 전 블라인드 평가 A/B 4차원 전승(register +0.55、structure +0.58).
- **deslop 과업 카뎃과 비유 밀도(#218)**: 과업 카뎃은 정보/감정/관계/대가/선택 압력/복선/훅(hook) 계승을 바꿀 때만 사용합니다; 신규 `metaphor-density-tic` advisory(처럼/마치/같다의 고밀도 중첩 검출); 주작은 보조 신호로 위치지우고, AI스러움 제거가 플롯 경계를 넘지 않게 합니다.
- **범용 Web AI 배포(#216)**: story-setup에 신규 `target_cli=generic` 파일 모드를 추가했습니다(`skills/` 복사 + 범용 `AGENTS.md`, 플랫폼 네이티브 hooks/custom agents는 선언하지 않음); `story-long-write`에 범용 환경 solo/direct 폴백을 보강했습니다.

### 개선

- **장편 워크플로우 제어 방지(#225)**: `story-long-write`를 노출 호출해도 자동으로 正文/일일 연재 모드로 진입하지 않습니다; 책 시작 흐름은 기본적으로 大纲에서 멈춥니다; 일일 연재 배치에는 한계가 있습니다; narrative-writer는 细纲 계획 내 플롯 포인트만 확장 쓰고, 부족할 때 `outline_underfilled` 미결산 리포트를 반환해 메인 세션이 细纲을 보충하도록 넘깁니다; setup → import → long-write의 연재 프로젝트 순서를 정리했습니다.
- **전체 키트 문서 슬리밍(#228)**: 13개 스킬(skill) 시스템 감사를 거쳐 행위 불변을 증명할 수 있는 중복——일대일/동의어 반복, 만료된 디렉터리, 무효한 행 번호 앵커, 유지보수성 주석, 장르 교차 데드 섹션, 댕글링 포인터——을 삭제해 53 파일 순 −32.9KB 달성했습니다; 동명 사본 그룹은 모두 바이트 동기화; Σ 글자 수 예산 계약, anti-leak, 훅(hook) 앵커는 전혀 건드리지 않았습니다.
- **deslop 검출기 게임화 방지(#220 / #221)**: 커뮤니티 반 AI 발상을 흡수하되 검출기 눈치 보기 하드 규칙은 넣지 않습니다; 신규 `action-list-tic` advisory(감시 렌즈식 동작 체인); 외부 검출기는 명시적으로 자가 검토 참고로 삼으며, 인간의 통독을 대체하지 않습니다; 주작 AIGC 검출 CLI 감사를 복원했습니다.

### 발표 준비

- 버전 번호를 `0.6.22`로 올렸습니다(`.claude-plugin/marketplace.json` + `skills/story/VERSION`). `.story-deployed`의 `agents_version`은 `17`로, `setup_skill_version`은 `1.2.6`으로 업그레이드했습니다; 본 버전에는 narrative-writer / chapter-extractor 배포 템플릿 업데이트(장르 카드 회수 + anti-leak + 大纲 경계와 chapter_formula)가 포함되어 있으므로, 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어 적용받아야 합니다. `UPGRADING.md`에 v17 항목을 신규 추가했고, `README` / `README_EN` 버전 설명은 최근 3판으로 수렴시켰습니다(더 이전은 CHANGELOG 참고).

## v0.6.21

> 단편 작성 참고 스택 슬리밍: 장편 상속 잔여분 삭제, 단편 전용 format/craft/deslop/장르 패키지 체계 확립(#206)

### 개선

- **단편 작성 references 정리(#206)**: `story-short-write`에서 장편 상속 잔여 참고 13개(캐릭터, 장편 구조, 범용 장르 독자, 시작 편, 상태 追踪 등)를 삭제해, 단편 작성이 계속 장편 규칙에 오도되지 않게 했습니다. 단편 전용 기반층을 신규 추가했습니다: `short-format.md`(하드 포맷/문단/문장 부호/正文 계약), `short-craft.md`(단편 서술 자세, 감정 직격+체감 용접, 삼차원 짜넣기, 밀도 제어), `short-deslop.md`(단편 AI스러움 제거, 진짜 AI 어조만 죽이고 감정 력도는 죽이지 않음).
- **장르 패키지가 단편 스타일을 관장(#206)**: 신규 `genre-styles/追妻火葬场.md`、`复仇打脸.md`、`总裁豪门.md`、`宅斗宫斗.md` 네 개의 단편 장르 패키지를 추가해, 시작 패턴, 쾌감 포인트 밀도, 대화 스타일, 감정 패턴, 결말 패턴을 장르 로컬로 수렴시켰습니다; 비인기 장르는 여전히 `genre-writing-formulas.md`로 폴백해 읽을 수 있습니다.
- **단편/작품 분석 AI 규칙 경계 명시화(#206)**: 공유 `output-contract.md`에서 작품 분석 리포트 품질 게이트는 여전히 `story-short-analyze/references/anti-ai-writing.md`를 거치고, 단편 완성고 AI스러움 제거는 `story-short-write/references/short-deslop.md`를 거치도록 명확히 해, 두 규칙 세트가 계속「로컬 AI 어조 규칙 파일」같은 암묵적 구문으로 커플링되지 않게 했습니다.
- **배포 템플릿 단편 예외 동기화(#206)**: `story-setup`의 narrative-writer Claude/OpenCode/Codex 템플릿에 단편 장르 패키지 예외를 동기화했습니다: 단편에서「감정어 + 체감/동작으로 용접」해야 할 때는 억지로 순수 동작 외현으로 고치지 않고, 공허하고 체감 없는 AI 감정 요약만 정리합니다.

### 발표 준비

- 버전 번호를 `0.6.21`로 올렸습니다(`.claude-plugin/marketplace.json` + `skills/story/VERSION`). `.story-deployed`의 `agents_version`은 `16`로, `setup_skill_version`은 `1.2.5`로 업그레이드했습니다; 본 버전에는 배포된 narrative-writer 템플릿과 reference 번들 업데이트가 포함되어 있으므로, 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어 적용받아야 합니다. `UPGRADING.md`에 v16 항목을 신규 추가했고, `README` / `README_EN`에 v0.6.21 버전 설명을 업데이트했습니다.

## v0.6.20

> 장편 大纲 보강: 대상/벤치마크 리듬 이관(죽은 링크 활성화) + 장 포지셔닝과 장력 완화(「매 장이 단편처럼」깨기)(#198)

### 신규

- **대상/벤치마크 리듬 이관(#198)**: 대상/벤치마크 책에서 분해한 `剧情/节奏.md`는 과거 Phase4 장별 문풍에만 먹히고 卷级 구조로는 역류하지 않았습니다; 신규「1급 구조=대상/벤치마크 책 플롯 유닛 + 키 포인트법 + 대상/벤치마크 리듬 이관 M1-M5」(`outline-structure-theory.md`)를 추가합니다. 卷纲 템플릿에「대상/벤치마크 구조 좌표」표를 추가하고, Phase3 卷纲 확정 전 대상/벤치마크 리듬 역류를 읽습니다(정규화된 키 포인트 1/4·중간점·3/4로 소재를 본 卷에 배치, 감정 체인+기능 자리 유지,桥段 그대로 베끼기 금지, 그리고「따라 하지 말기」를 대조해 대상/벤치마크 고질병을 그 자리에서 수정). `节奏.md`/`情绪模块.md`가 없을 때는 `章节摘要 + 拆文报告` 폴백 경로로 진행합니다(대부분의 실제 작품 분석库에 이것들이 없어, 盘龙 작품 분석库에서 종단간 검증 완료했습니다).
- **장 포지셔닝과 장력 완화, 「매 장이 단편처럼」깨기(#198)**: 최상층「매 장을 동등하게——훅(hook)+쾌감 포인트+현안」으로 모델이 관계/복선/장면 넘김 장까지 모두 강한 훅(hook) 달린 단편으로 쓰게 몰았습니다; 이제 장이 1급 구조 안에서 차지하는 위치에 따라 여섯 가지 유형의 포지셔닝으로 나눕니다(고압/추진/수련 시행착오/관계 회수/저압 일상/정보 정리). 저압/관계/장면 넘김 장은 명시적 쾌감 포인트 없고 약한 훅(hook)을 허용하되, 매 장마다 다음을 읽을 이유는 남깁니다(판치에(番茄小说) 추독 레드라인). 그리고「감정 모티브 몰려 나오기 금지」하한을 추가합니다(여러 장 연속으로 같은 모티브가 진짜 피로의 원인). 포지셔닝은 비워둬도 됨=추진 장으로 폴백, 하드 쿼터도 아니고 새 교리도 아닙니다; 압력级=폭발 력도로, 감정 강도와는 두 축으로 나눕니다; 비율은 장르별로 등급을 나눕니다(판치에(番茄小说) 단평고속 고압은 30%+까지 가능). SKILL.md 大纲 4검사를 5검사로 올리고, 최상층 품질 요구/쾌감 포인트 밀도/step9/세 군데 QA를 포지셔닝에 따라 완화했습니다. story-architect spawn에「장 포지셔닝 계약」을 인젝션하고, `agent-references/quality-checklist.md`의 누락 동기화를 수정했습니다(배포된 에이전트(agent) 심사 시 실제로 읽음). 블라인드 평가(같은 새 장을 구/신 규칙으로 각각 한 판씩 正文 작성): 구판은「공업적 쾌감 소설/매 장마다 훅(hook)이 필수인 컨베이어 벨트 산물」판정을, 신판은「관계 장 작가형 쓰기」판정을 받았으며, 편집자 판정상 신판이 클라이맥스 후 호흡 자리에 훨씬 적합했습니다; 글자 수 예산 Σ∈[장 목표, ×1.1] 계약은 움직이지 않았고, 관계 회수 장에「≥1 감정 밀점」더 엄격한 하한을 추가했습니다.

### 발표 준비

- 버전 번호를 `0.6.20`으로 올렸습니다(`.claude-plugin/marketplace.json` + `skills/story/VERSION`). 본 버전은 `agent-references/quality-checklist.md`와 `outline-methods.md`의 장 포지셔닝 면제를 동기화 업데이트하고, `story-architect` spawn 프롬프트(prompt)에「장 포지셔닝 계약」을 인젝션했습니다——배포 에이전트(agent)는 재배포 없이도 새 포지셔닝으로 大纲을 배치/심사합니다. `agents_version`을 bump하지 않았습니다(본 버전에는 hooks/agents/agent 템플릿 구조 변경이 없고, agent-references 내용은 새 프로젝트 배포나 다음 재배포 시 자연스럽게 적용됩니다). `README` / `README_EN`에 v0.6.20 버전 설명을 업데이트했습니다.

## v0.6.19

> Codex CLI 어댑트 + OpenClaw 호환(#186) · 사용자 정의 문풍 `设定/文风.md`를 대상/벤치마크보다 우선(#194) · 모델 열화/엔지니어링 용어 유출 검출(#173) · 조각마침표/긴 문단 검출 + 대시를 기능에 따라 재작성(#188) · 正文 보충 가드 + 배치 간 연속성 확정성 망(#195) · OpenCode 자식 에이전트(agent) 모델 자동 설정으로 비용 절감(#191) · 细纲을 글자 수 예산으로 편성해 글자 모자람 반복 재가공 근절(#187)

### 신규

- **Codex CLI 어댑트 + OpenClaw 호환(#186, #189)**: `$story-setup`이 `.codex/agents/*.toml`(Claude agent 템플릿에서 `generate-codex-agents.py`로 생성)과 `.codex/hooks.json`을 배포합니다; Codex가 repo를 바로 쓸 때 `.agents/skills`(Codex와 공유하는 `skills/`로의 symlink, #189에서 symlink 가드를 추가하고 Windows `core.symlinks` 함정을 기록)를 스캔합니다. OpenClaw skills-only 호환을 보강하고(단행 frontmatter + `metadata.openclaw` + `story-setup target_cli=openclaw`), OpenCode/Codex 어댑트 표류 가드를 강화했습니다(`check-opencode-adapter.sh` / `check-codex-adapter.sh`).
- **사용자 정의 문풍을 대상/벤치마크 문풍보다 우선(#194)**: 매 장 작성 전에 먼저 `设定/文风.md`를 읽고, 실질 내용이 있으면「사용자 정의 문풍 모드」로 진입합니다——이것이 권위 스타일 베이스(문장 길이 / 소프트 문장 부호 / 대화 잠재 대사 / 감정 교대)가 되며, 대상/벤치마크 / 작품 분석 `文风.md`는 참고로 격하됩니다(원문 앵커 + 문장 길이 보충 가드). `narrative-writer` 문풍 핑거프린트에「출처」필드를 신규 추가해, 사용자가 `设定/文风.md`를 새로 만들거나 고친 뒤에는 새 출처로 문장 길이 대역 스냅샷을 갱신하도록 해 더는 이전 대상/벤치마크에 영구히 눌리지 않게 했습니다(삼단말 템플릿 + `上下文.md.tmpl`).
- **모델 열화 + 엔지니어링 용어 유출 검출기(#173)**: 신규 `check-degeneration.js`(4개 바이트 동기 사본)을 추가해, 약한 모델 열화를 확정성 검출합니다——일대일 반복/제자리 돌기, 말미 절단, 플레이스홀더/거절 어구(`作为AI`/`我无法续写`/`（此处省略）`/깨진 문자 �), 엔지니어링 용어가 正文으로 새는 것(`细纲`/`情节点`/`本章`/`下一章` 등). 각 finding마다 `severity: blocking|advisory`를 달았습니다(blocking은 바로 재쓰기, tier2 장/다의어 단어는 알림만, 대화행의 tier1 엔지니어링 용어는 advisory로 격하). `story-long-write`/`story-deslop`/`story-review`/`story-short-write` 마무리 복스캔에 연결했고, `story-review` 자식 에이전트(agent) 프롬프트(prompt)에「승계된 미결 항목」을 보강해 배치 간 연속성을 확보했습니다.
- **조각마침표/긴 문단 검출 + 대시를 기능에 따라 재작성(#188)**: `check-ai-patterns.js`(4개 바이트 동기 사본)에 조각마침표(연속된 짧은 서술문에 호흡 없음), 긴 문단(>200자는 렌즈별로 문단 나누기) 검출을 신규 추가하고, 대시를 기능에 따라 재작성 제안을 했습니다(끼어들기→동작 beat/짧은 문장, 길게 늘어짐→줄임표나 동작, 삽입 설명→쉼표/콜론, 일률적으로 마침표로 고치지 않음). 각 finding마다 `severity`를 달았고, 혼합행(서술 + 따옴표 안 물건)이 한 개 따옴표로 행 전체 면제되는 일을 없앴습니다. `story-review`에서 em-dash를 `check-ai-patterns.js`로 귀속시키고 normalize와 중복을 제거했습니다.
- **正文 보충 가드 + 배치 간 연속성 확정성 망(#195)**: 신규 배포 훅(hook) `check-prose-after-write.sh`(PostToolUse Write/Edit 디스크 낙하 후 하드 신호 보충 가드——절단, 거절 어구/AI 자기지시, 엔지니어링 용어 유출, 행별 반복, 글자 수 미결산)를 추가했습니다. 메인 세션이 확정성 마무리를 빼먹어도 받아낼 수 있습니다; 삼단말(Claude/OpenCode/Codex) 경량 망 parity 가드를 뒀으며, Codex는 Stop 회차 말미 git 변경 집합으로 스캔합니다. 배치 간 연속성은 세션 시작에서 연재 단절 / 장 이름 충돌을 알립니다.
- **OpenCode 자식 에이전트(agent) 모델 자동 설정(#191)**: `$story-setup`에 `target_cli=opencode`가 포함되면 `opencode models`를 검출하고 등급에 따라 각 작성 에이전트(agent)에 `model:` 필드를 써 넣어, 저비용 에이전트(agent)가 메인 모델을 상속해 고액 소모되는 것을 피합니다. 단계별 AskUserQuestion으로 선택하게 하며, 사용자 정의 입력/기존 값 유지/건너뛰기를 지원합니다. `opencode models --verbose`의 비용 등급을 우선하고, 키워드를 폴백으로 삼습니다.

### 개선

- **细纲을 글자 수 예산으로 편성해 正文 글자 모자람 반복 재가공 근절(#187)**: 细纲 플롯 포인트 시퀀스를 글자 수 예산에 따라 편성하도록 변경했습니다——각 포인트에 밀/소로 예산을 표시하고(밀 ≥250, 슬로우 모션 쾌감 포인트 400-600, 소 ≈40, 복선 120-150), 각 포인트 합 Σ가 [장 목표, 장 목표×1.1]에 들어가게 합니다. 쓰기 후 글자 수 검증은 예산과 대조해 미결산 밀포인트를 특정해 한 번에 재작성해 끝내며, 포인트별로 조금씩 짜내며 반복 재가공하지 않습니다. 그리고 > 장 목표×1.1의 초과 예산 수렴 분기를 보강했습니다; `story-architect`를 호출할 때 spawn 프롬프트(prompt)에 예산 계약을 인젝션하고 细纲 수령 후 Σ를 검증합니다.

### 수정

- **大纲 가드가 Windows 드라이브 문자 절대 경로를 인식(#184)**: 正文 쓰기 전 大纲 가드 훅(hook)이 `C:\\...` 형태의 Windows 드라이브 문자 절대 경로를 올바르게 인식해, Windows에서 正文 파일 경로를 오판하지 않게 했습니다.

### 발표 준비

- 버전 번호를 `0.6.19`로 올렸습니다(`.claude-plugin/marketplace.json` + `skills/story/VERSION`). `.story-deployed`의 `agents_version`은 `15`로, `setup_skill_version`은 `1.2.4`로 업그레이드했습니다——본 버전에는 배포 훅(hook) / 에이전트(agent) 템플릿 / Codex 어댑트 변경이 포함되어 있으므로, 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고 새 세션을 열어 적용받아야 합니다. `UPGRADING.md`에 v15 항목을 신규 추가했고, `README` / `README_EN`에 v0.6.19 기념사를 업데이트했습니다.

## v0.6.18

> OpenCode CLI 완전 지원(#151) · 내장 버전 갱신 알림(#173) · 대화 기계화/논문 어조 수정(#171) · 연재 문풍 표류를 매 장마다 자가 검사(#168) · 신규 명사 앵커(#175) · AI 문장 하드 문턱과 detector 복스캔(#166) · 표지 플랫폼 크기 크롭 보충 가드(#176) · Windows 중문 시스템 훅(hook) 바이트 안정화(#164)

### 신규

- **OpenCode CLI 완전 지원(#151)**: 신규 `skills/story-setup/references/opencode/` 어댑트 계층을 추가했습니다(`plugin.ts` 正文 쓰기 전 大纲 가드, 13개 command 래핑, `AGENTS.md.tmpl`, `pre-commit.sh`, `opencode.json.patch`). `scripts/sync-opencode.py`가 Claude agent 템플릿에서 opencode 사본을 자동 생성하고 `.github/workflows/sync-opencode.yml`(check-sync)이 가드합니다; 6개 작성 스킬(skill) + story-setup의 에이전트(agent) 검출이 `.claude/agents/`와 `.opencode/agents/`를 동시에 호환하며, 에이전트(agent) 참고 경로 파싱에 `.opencode/skills/` 단계를 추가했습니다.
- **내장 버전 갱신 검사(#173)**: 새 스킬(skill)을 추가하지 않고, 갱신 검사를 `/story` 라우팅에 접어넣었습니다. 능동「버전 검사/갱신」은 `skills/story/VERSION`을 읽고 GitHub 최신 release와 비교해, 현재→최신 + Releases/CHANGELOG를 알린 뒤 AskUserQuestion으로 갱신 여부를 결정합니다(알림만 하고 자동 설치는 하지 않음); session-start에 수동 알림을 추가했습니다(24h에 최대 한 번, curl 5s 타임아웃, 전체 침묵 폴백, `STORY_NO_UPDATE_CHECK=1`로 끌 수 있으며, 뒤처질 때만 알림). 수동 알림은 배포 훅(hook)을 거치며 Claude Code에만 적용됩니다; 능동 검사는 두 단말 공통입니다.

### 개선

- **대화 기계화/논문 어조/자리 구분 없이 수정(#171)**: `narrative-writer` 참고표에 `dialogue-mastery`(필요할 때 읽기)를 연결했습니다. 심사 목록에 대화 품질 항목별(기계적 대화/캐릭터 과학설명 입/말하는 데 자리 구분 없음)을 신규 추가하고,「다 쓴 뒤 대화 자가 검사」마무리 단계를 추가해 대화 검사가 매 장마다 반드시 돌아가게 했습니다; 쓰기 전 의도 확인(`story-long-write` 3.3 / `workflow-daily` 2.4)에「③ 대화 성선 기준선」을 신규 추가해 본 장의 톤에 따라 수렴시킵니다(고압 beat→웃긴 성선은 양보, 정보형 조연은 과학설명 입 하지 않기, 상대방 감정을 한 마디씩 계승). `story-review` Agent2(character-designer)에 대화 삼증상 독립 심사를 신규 추가했습니다. `dialogue-mastery.md`(4개 동기 사본)에「매 마디가 위 마디 감정에 응답하기」「Gate G가 대사도 마찬가지로 관장하기」「자리 예외 성선 양보」를 보강했습니다.
- **연재 문풍 표류(쉼표 더듬이체)를 매 장마다 자가 검사(#168)**: `narrative-writer`에「다 쓴 뒤 문풍 자가 검사」마무리 단계를 신규 추가해, 목표 문장 길이 대역으로 본 장의 문단 길이/조각 문장 비율을 대략 측정하고, 표류 시 목표 대역에 따라 조각 문장을 중장문으로 합쳐 다시 씁니다(`文风.md`/원문 앵커를 기준으로 하며, 이미 표류했을 수 있는 이전 장을 따라하지는 않습니다); 목표 문장 길이 대역 스냅샷을 컴팩션 저항성 있는 `追踪/上下文.md`의 신규「## 문풍 핑거프린트」구역에 박아 넣습니다(첫 작성 시 `文风.md`에서 스냅샷을 채워 넣은 뒤 그대로 유지). 자가 검사는 우선적으로 문맥 핑거프린트로 표류 여부를 판단해, 긴 세션 컴팩션 후 문풍 경로를 잃는 사각지대를 메웁니다.
- **신규 명사/设定 첫 등장 시 독자에게 앵커 달기(#175)**: `anti-ai-writing.md`(6개 동기 사본) Gate G/패턴8 자가 검사 뒤에 역방향 견제를 보강했습니다.「설명 어조 지운다 ≠ 독자를 헷갈리게 한다」——신규 명사/设定이 첫 등장할 때는 여전히 캐릭터 동작 반응/대화 반 마디/장면 물리적 결과로 한 번에 당장의 작용을 훑어서 보여주며, 유래를 한 단락 쭉 늘어놓거나 정보 제로인 낯선 단어를 던지지 않습니다. 그리고「블루 크리스탈」예시를 들어「앵커 vs 설명」을 구분했습니다.
- **AI 문장 하드 문턱 + detector 복스캔(#166)**: `narrative-writer`、작성 스킬(skill)、review/deslop 흐름에서「먼저 부정했다가 긍정하는」반전 문장을 하드 금지로 분류했습니다(문풍 회수/대상/벤치마크 모방/Gate B 소프트 규칙도 덮어쓸 수 없음); `story-deslop`/`story-long-write`/`story-short-write`/`story-review`에 로컬 `check-ai-patterns.js`를 동봉해, 파일 모드에서 사전 검사/납품 전 `node scripts/check-ai-patterns.js --check <正文文件…>` 복스캔으로 0까지 만들었습니다; narrative-writer에 Bash 도구가 없을 때는 자가 검사했다고만 보고하지 스크립트를 돌렸다고 거짓말하지 않습니다. detector 오보를 수렴시켰습니다: `是`-류 접속사(只是/可是/于是…)、either-or「A가 아니면 B다 / B이기도 하다」와 문말 반문「…, 맞지 / 그렇지 / 그렇죠」가 더는 반전 문장으로 취급되지 않게 했습니다(4개 동기 사본 + 회귀(regression) 유즈 케이스).
- **표지: 필명 강제 수집 + 플랫폼 사이즈는 크롭 보충 가드로(#176)**: `story-cover` Step 1에서 책이름/필명이 부족하면 먼저 AskUserQuestion으로 보충하고 비워두지 않습니다; `GPT_IMAGE_SIZE`는「목표 비율 안내」로 격하했습니다(실측 대부분의 중계/집계 프록시가 size를 무시하고, 어떤 요청에도 약 2:3 세로 그림을 돌려주었습니다); 신규 Step 3.5 중앙 크롭+스케일(crop-to-fill)을 추가해, 출력 비율과 상관없이 확정적으로 플랫폼 정확 픽셀(판치에(番茄小说) 600×800)으로 잘라냅니다. 왜곡 없고, 프록시가 size를 인식하는지 의존하지 않습니다; 프롬프트(prompt) 책이름/필명은 중앙 안전 구역(inner ~85%)에 남겨둡니다.

### 수정

- **Windows 중문 시스템 훅(hook) 바이트 안정화(#164)**: GBK 지역 코딩의 두 가지 함정을 근절했습니다——python stdout는 언제나 cp936이었습니다(`sys.stdout.buffer.write`로 수정); 내보내기 GBK에서 gawk/sed/grep/bash 와일드카드가 UTF-8을 mis-decode했습니다(7개 배포 훅(hook)에 통일적으로 `export LC_ALL=C`(python-safe하게 배치), `common.sh` book-name sed를 감싸기, 전각 콜론을 `(：|:)` 교대로 처리, 전각 스페이스 U+3000 처리). 진짜 GBK 지역의 종단간 CI(ubuntu/windows/macos) + 정적 locale-safety 가드 + python `print()` 금지를 신규 추가했습니다.
- **글자 수 통계 오류(#170)**: 글자 수 통계를 수정했습니다; `narrative-writer` Gate E에「구체적 글자 수 표현 검증」을 추가했습니다(正文中 스크립트 검증 없이「이 다섯 글자」류 글자 수 단언을 금지하고, 비숫자 표현으로 바꿈).

### 문서

- **README OpenCode 주의사항 간소화(#179)**: README / README_EN에서 #151이 도입한「OpenCode 알려진 차이」5개 bullet을 한 줄로 압축했습니다(설치 발견 + 재시작 + 첫 자연어 트리거 + 「일부 훅(hook) 동작에 차이가 있음, 자세한 것은 CONTRIBUTING 참고」). 구현 세부 사항은 일관되게 `CONTRIBUTING.md`로 되돌려 채웠습니다(detect-gaps/session-end/validate-commit/browser-cdp ESC). 포인터가 이름값을 하며 정보를 잃지 않습니다.

### 발표 준비

- `CHANGELOG.md`에 v0.6.18 항목을 신규 추가했습니다; `.claude-plugin/marketplace.json` metadata.version을 0.6.17 → 0.6.18로 올렸습니다; `skills/story/VERSION`을 0.6.17 → 0.6.18로 올렸습니다(갱신 검사 앵커로, release 태그와 일치해야 함).
- story-setup `setup_skill_version` 1.2.3 / `agents_version` 14(v0.6.17 이후 이미 bump, 이번에 변경 없음). templates/hooks/rules/references가 업데이트되었으므로(OpenCode 어댑트 계층, 대화/문풍 자가 검사, detector 복스캔, 수동 갱신 알림 포함), 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고, 배포 후 새 Claude Code 세션을 열어야 합니다.
- canonical source는 `skills/**`、`scripts/**`、`CHANGELOG.md`와 `.claude-plugin/marketplace.json`에 위치합니다; 루트 디렉터리 `.claude/`는 여전히 무시되는 로컬 배포 미러로 취급합니다.

## v0.6.17

> 사용자 피드백 특별: 장편 细纲을 장 청사진으로 업그레이드(#162) · 어조 문장 부호 계보(#161) · story-setup v13 배포 갱신 · v0.6.16 이후의 깊이 제한 시점, 正文 메타 정보, 작품 분석 모듈 체인, review 일치성, 문단/주어 리듬 등 수정 내용 합류

### 신규 / 개선

- **장편 细纲을 장 청사진으로 업그레이드(#162)**: `story-long-write`의 `大纲/细纲_第XXX章.md`는 기존 필드(핵심 사건, 목표 감정, 장首/장尾 훅(hook), 쾌감 포인트, 글자 수 목표)를 유지하고, 내용 개요(발단/전개/전환/클라이맥스/结尾), 플롯 편성(주선/보조선/사건선/감정선/논리선), 인물 관계와 등장 순서, 플롯 세분화, 结尾设定과 훅(hook)을 신규 추가했습니다.
- **일일 연재와 가져오기가 신판 청사진을 소비**: 일일 연재 준비층에서 내용 개요, 다선 편성, 캐릭터 등장, 대가/수익과 장尾 계승을 명시적으로 읽습니다; 구판 细纲에 신판 필드가 없어도 막히지 않으며, 보충/역방향 채우기로 모르는 항목은 일관되게 `[待补充]`로 씁니다; `story-import`는 증거에서만 신판 필드를 역추론하며, 보조선이나 관계를 지어내지 않습니다.
- **细纲 청사진의 상류·하류 통관(#162 하류 세분화)**: 기존 craft reference를 일일 연재 작성/대조 폐쇄 루프에 연결했으며, 기존 필드를 재사용하고 새 개념은 제로로 했습니다. `内容概括`의 전개/전환은 `plot-emotion-system` 역추론법으로 쾌감 포인트 복선을 깔고(안 깔면 공허해짐); `视角/信息差`는 `出场顺序`의 현존 조연으로 차별화 반응을 확대합니다(`plot-core-methods` 집단 충격); `情节点功能标签`는 곧 `commercial-core-methods` 목적어로, 펼치기/속도조절 상세를 구동합니다. `story-long-write` 쓰기 전 준비(3.3/2.4) + 细纲 대조 확인(각 항목마다 수정 동작 포함)에 반영했으며, `story-architect` 템플릿의 명칭도 일치시켰습니다; 새 필드/파일은 추가하지 않으며, 구 细纲도 여전히 비차단입니다.
- **단편 소절 大纲 경량 강화**: `小节大纲.md`에 구조 단락/오단락 기능, 캐릭터/관계 변화, 인과/논리 체인, 결말 계승/훅(hook) 등 필드를 추가하되, 장편 완전 장 청사진을 덧씌우지 않아 단편 흐름이 무거워지지 않게 했습니다.
- **용어 백화(자조 은어 삭제, 의미는 불변)**: 작가 지향의 난해한 자조/차용 복합어를 백화로 바꿨습니다. 청사진 대조 확인→细纲 대조 확인, 삼차원 짜넣기→삼차원 녹여넣기, 렌즈 문단 나누기→화면 분할, 최소 기억 꾸러미→본절 속기, 품질 게이트→품질 검사, 준비층→쓰기 전 준비, 캐릭터 지位 추상→기능位로 간주, 오중 구동→다섯 가지 구동, 고립 플롯 보충 가드→흩어진 플롯 보충 가드, 구조 소품→관통 아이템, 삼현 편성→세 번 등장, few-shot→예제 단편, 목적 함수→목표, 글자 수 프로브→글자 수 검사 등; 功能位/信息团/目的词/드라마틱 유닛/疏密 할당 등 업계 실제 용어는 유지; 스킬(skill) 간 바이트 동형 사본을 동기화해, 로컬 가드가 모두 녹색 신호입니다.
- **어조 문장 부호 계보(#161)**: writer references、`narrative-writer`、`story-review`、`story-deslop`에「문장 부호는 어조/캐릭터 성선에 복무한다」규칙을 추가했습니다: 질문은 물음표, 폭발 피크는 드문 느낌표; 망설임/미완/끼어들기/길게 늘어짐은 동작 멈춤, 짧은 문장이나 줄 바꿈으로 처리하고, 正文 산출물에 `……` / `——`를 더 이상 쓰지 않습니다; 동시에 통째로 마침표화하는 것과 무작위 문장 부호 쌓아놓기를 명시적으로 금지합니다.
- **story-setup v13**: `setup_skill_version`을 `1.2.2`로, `.story-deployed`의 `agents_version`을 `13`으로 업그레이드했습니다; `story-architect`가 신판 장 청사진을 산출하고, `consistency-checker`가 논리선/캐릭터 관계 변화/등장 순서/대가 대조를 소비합니다. session-start에서 v12 이하에게 재배포를 안내합니다.
- **v0.6.16 이후 병합된 개선 총괄**: 正文 쓰기 전 细纲 가드와 subagent solo 다운그레이드 수정(#148), 깊이 제한 시점/설명 어조 지우기/감정 력도(#147/#152), 正文中 장 메타 정보 금지(#155), 부 대상/벤치마크 책 수량 완화(#157), story-review 추론형 일치성 검사(#158), 작품 분석→재사용 가능 작성 모듈 체인(#149/#156), 만료된 시장 가이드 청소(#160), 문단/주어 리듬 자연화(#159)를 포함합니다.

### 수정

- **스킬(skill) 지시 일치성(서로 상충하는 수렴)**: `story-long-write` 일일 연재 읽기 목록과 `workflow-daily.md`의 `剧情/情绪模块.md` / `剧情/节奏.md` 누락 문구를 통일했습니다——v12 신규 계약 누락 시 멈춰서 수정하되, 레거시 폴백만 허용하며, 권위「누락 파일 폴백」규칙과의 충돌을 없앴습니다; `story` 라우팅의「스토리 자료 찾기 / 자료 찾기」를 먼저 경량 에이전트(agent) 가용성 검사를 하도록 바꾸고, 쓸 수 없을 때는 직접 Read/Grep으로 다운그레이드하며 `Fallback: agent unavailable -> direct lookup`라고 표시해, 무조건 바로 spawn하지 않게 했습니다.
- **문장 부호 정규화기 경계 수정**: `normalize-punctuation.js`에서 열린 따옴표 / 열린 괄호(`「『（(“‘`) 바로 뒤의 `……` / `——`를 빈 삭제 처리해, `「……你回来了。」`가 `「，你回来了。」`로 잘못 바뀌거나 `「……」`가 `「。」`로 바뀌는 문제를 수정했습니다; 문말 늘어짐, 숫자 구간, 콜론 낙하지점 등 기존 동작은 그대로입니다(4개 사본이 바이트 일치를 유지).

### 발표 준비

- `CHANGELOG.md`에 v0.6.17 항목을 신규 추가했습니다; `.claude-plugin/marketplace.json` metadata.version을 0.6.16 → 0.6.17로 올렸습니다.
- story-setup templates/rules/references가 업데이트되었으므로, 이미 배포된 프로젝트는 `/story-setup`을 다시 실행하고, 배포 후 새 Claude Code 세션을 열어야 합니다.
- 루트 디렉터리 `.claude/`는 여전히 무시되는 로컬 배포 미러로 취급하며 발표 소스로 삼지 않습니다; canonical source는 `skills/**`、`scripts/**`、`CHANGELOG.md`와 `.claude-plugin/marketplace.json`에 위치합니다.

## v0.6.16

> 순위 스캔 전 플랫폼 견고성 실측 수정: 판치에(番茄小说) 책이름 전체 폴백 `bookId:xxx` 수정 + 장르/태그 확대 채집 · 점중(点众)/치마오(七猫)/자웨묘(刺猬猫) 책이름과 작품 페이지 링크 수정 · 흑암(黑岩) 오류 상태 세분화 · 진장(晋江) 상세 페이지 핵심 지표 채집 보강 · 전 플랫폼 연결성 자가 진단/품질 신호 · 분해 파이프라인 합법성 문맥 · 작성 흐름 대시 필터링 · prompt-cache 최적화

### 버그 수정(순위 스캔)

- **판치에(番茄小说) 순위 스캔 책이름 전체 폴백 `bookId:xxx` 수정**: 근본 원인은 상세 페이지 디코딩이 페이지 전체 약 20권을 한꺼번에 동기 XHR을 하나의 eval에 욱여넣어, `cdp-utils.ab()`의 20s 하드 타임아웃에 걸려 → 침묵하며 공백을 반환 → 권마다 bookId로 폴백했던 것입니다. 이제 배치 디코딩(5권마다) + 다중 전략 파싱(내장 JSON `bookName` / `<title>` / og:meta)으로 바꾸고, 연결성 자가 진단과「제목 파싱률 / 데이터 품질」파일 헤더 표기를 추가했습니다(#144).
- **점중(点众) 순위 스캔 책이름이 UI 텍스트/소개 문자열 → 재작성**: 이제 `bookId`로 앵커를 집계 파싱합니다(책이름은「책이름+평점」앵커에서 꼬리 `X.X分`를 떼고, 소개는 가장 긴 앵커를 취하며, 작가/상태/글자 수는 카드 텍스트에서 가져옴). 실측 책이름 10/10, 작품 페이지 링크 10/10(#144).
- **치마오(七猫) / 자웨묘(刺猬猫) 작품 페이지 링크가 거의 다 사라진 문제 수정**: `extractBookUrls` 구판이 bookId로 가져온 것이 순위 숫자 / 빈 표지 앵커를 책이름으로 삼아 역채우기에 실패했었습니다; 이제 가장 책이름 닮은 앵커 + 책이름 귀일 역채우기로 바꿔, 실측 링크 20/21、10/10; 치마오(七猫) 채널 탭 클릭 실패는 자동으로 한 번 재시도합니다(#144).
- **흑암(黑岩) 순위 스캔 오류 상태 오보 수정**:「인터페이스 타임아웃 / CDP 끊김」「401 미인증」「서버측 오류 코드」를 나눠 오류를 보고하며, 더는 일률적으로「인증 실패」+ DOM 선택기 구문을 적용해 오보하지 않습니다; 책이름 적중률 품질 게이트를 추가해, 필드명이 바뀌면 차단하지 정적으로 undefined를 쓰지 않습니다(#144).
- **분해 파이프라인 소재 합법성 문맥 보강**: 사용자 소유 작품에 대한 과도한 거절을 없앴습니다(#143).
- **장편 작성 흐름 대시 필터링**: 대시를 자동 필터링 + 정규화기가 합법적인 대시를 잘못 건드리는 것을 수정했습니다(#139 / #141).

### 개선(순위 스캔)

- **진장(晋江) 상세 페이지 채집 보강**: 목록에서 책이름 / 작가 / `novelid`를 취한 뒤 `onebook.php` 상세 페이지로 들어가, `fetch + TextDecoder('gb18030')`로 `itemprop` 마이크로 데이터를 해제합니다(찜하기 / 영양제 / 적립금 / 글자 수 / 상태. 공개 지표는 로그인 불필요); `--top` / `--detail-limit`로 물량을 제어하고, `--list-only`로 건너뛸 수 있습니다(#144).
- **판치에(番茄小说) 장르 / 태그 확대 채집**: 장르는 상세 페이지 `categoryV2`의 첫 번째 `Name`을 취하고, 태그는 소개 앞머리 `【…】`를 취합니다(판치에(番茄小说) SSR에는 평점 필드가 없어 평점 선언을 제거했습니다)(#144).
- **전 플랫폼 순위 스캔 견고성 통일**: 브라우저형 스크립트는 통일적으로 연결성 자가 진단(CDP가 안 켜짐 / 리다이렉트 당함 → 조작 가능한 오류로, 오해 사기 쉬운「구조가 바뀜」대체), 복잡한 eval은 base64 경유(shell 이스케이프 함정 제거), 출력 파일 헤더에 품질 신호(링크 / 책이름 / 제목 파싱률, 상세 적중률)를 추가했습니다(#144).

### 성능

- **prompt-cache miss 삭감**: `story-deslop` / `narrative-writer` / `story-long-analyze` 분해 파이프라인의 프롬프트(prompt) 캐시 미스 최적화(#142).

### 설명

- 순위 스캔 수정은 모두 실제 사이트 실측(headless Chrome을 격리해 플랫폼별로 돌려 통과) + 샌드박스 테스트(판치에(番茄小说) 31 / 진장(晋江) 10 단언)로 검증했습니다; `cdp-utils.js`는 움직이지 않았으며, 스킬(skill) 간 이중 사본은 여전히 바이트 일치합니다.
- 로컬 가드(shared-files / static-check 등)는 모두 녹색 신호입니다.
- marketplace metadata.version을 0.6.15 → 0.6.16으로 올렸습니다.

## v0.6.15

> 작품 분석 demo 전체 재작업(반룡 장편 / 증장애의사랑 단편) + 신규 story-import 장편 연재 프로젝트 demo · story-import 프레임워크 수정(납품물＝작성 프로젝트, `[导入反推]` 삭제) · 작품 분석 계약/게이트 보강 · story-deslop/story-review 문장 부호 정규화(즈후 옌옌(知乎盐言) 「」는 유효 유지)

### 개선

- **story-import(납품물＝작성 프로젝트)**: 시작편과 원칙 1에서「납품물은 연재 가능한 작성 프로젝트」임을 명확히 합니다——`작품 분석库/`는 프로젝트의 일부이며(`대상/벤치마크/`에 넘김), 쓰고 버리는 중간 산출물이 아닙니다; Phase 1에 신규「1.0 의도 확인」을 추가해, 사용자 의도가 불명확할 때「작성 프로젝트 짓기 vs 작품 분석库 분석만 원함」을 능동적으로 묻고 분기합니다(분석만 원하면 바로 `/story-long-analyze`로 진행).
- **`[导入反推]` 약정 삭제**: story-import의 기존「원칙 3: 가져온 출처 표기」와 모든 `[导入反推]` 표시/검증 항목을 삭제했습니다. 불확실한 필드는 일관되게 `[待补充]`로 바꿉니다(`SKILL.md` + `structure-mapping-long/short` + `character-state-reverse` 동기화).
- **story-long-analyze 작품 분석 계약 보강 + 기조/주제 태그 열거 확장**(#136).
- **story-short-analyze 게이트/집계 기준 보강**: 플롯 노드 집계 기준을 명확히 하고(복합 합병은 공통으로 하나의 N 번호를 씀, 밀도 검증은 최종 N 번호 총수로 계산); Phase 7.1 AI 어조 자가 검사에 원문 면제 규칙을 보강했습니다(`>` 인용행과 표 원문 직접 인용 열을 건너뛰고, 분석가 본인의 표현만 스캔)(#136).
- **banned-words 가장 독한 문장에 변형체 보강**:「A가 아니라, (오히려) B다」에서「(오히려)」를 생략해도 적중으로 보도록 표기했습니다(6개 동기 사본도 함께 업데이트)(#136).

### 버그 수정

- **문장 부호 유도 보정(Issue #133)**: `story-deslop` / `story-review`에 각자 내장된 확정성 대시/구분선 정규화기 `normalize-punctuation.js`(스킬(skill) 내에 한 부씩 복사, 스킬(skill) 간 참조하지 않음); 즈후 옌옌(知乎盐言) 단편「」따옴표는 유효 유지하며, 전역으로 오판하지 않습니다; 작성 references의 프롬프트(prompt) 예제에서「em-dash 리듬을 최우선 산문 모델로 삼기」가르침을 삭제했습니다.

### Demo와 문서

- **작품 분석 demo를 신규 계약으로 전체 재작성**: `demo/작품 분석库-반룡`(장편 작품 분석)、`demo/작품 분석库-曾将爱意私藏`(단편 작품 분석, 원문이 누락된「섀도우 복서」demo 교체).
- **신규 story-import 장편 연재 프로젝트 demo**: `demo/让你管账号，你高燃混剪炸全网`——판치에(番茄小说) 첫 20장을 역방향 재구성해 연재 가능 프로젝트로 만듭니다(正文 / 设定 / 大纲 / 追踪 / 참고 자료). 바로 `/story-long-write` 일일 연재로 제21장을 이어 쓸 수 있습니다.
- **README / README_EN**: 세 가지 demo 전시 블록(단편 작품 분석 / 장편 작품 분석 / 장편 연재 프로젝트) + 교류 그룹과 Discussions 링크를 신규 추가했습니다(#131).

### 공학

- **check-shared-files 가드가 동명 script 사본을 감시**: 스킬(skill) 간 동명 스크립트(예: `normalize-punctuation.js`)는 바이트가 반드시 일치해야 하며, 복사 사본이 표류하는 것을 막습니다.

### 설명

- 동명 공유 파일 변경은 모두 `check-shared-files.sh`에 따라 바이트 단위로 전체 사본에 동기화했습니다; 로컬 오중 가드(shared-files / python-invocation / story-setup-deployment / hook-regex-sync / static-check)는 모두 녹색 신호입니다.
- marketplace metadata.version을 0.6.14 → 0.6.15로 올렸습니다.

## v0.6.14

> 细纲 뒤에 신규 设定/캐릭터를 자동 보충(设定 표류 방지) · Windows `python3` 크로스 플랫폼 수정(Store 플레이스홀더 프로그램 exit 49) · SessionStart 훅(hook) 중문화 · 문서 보정(README_EN / CONTRIBUTING) · 공학 가드 강화(python 호출 / 구문 / 공유 파일 정밀도)

### 개선

- **story-long-write(细纲 뒤에 자동으로 파일 만들기)**: Phase 3 细纲 단락에 신규「细纲 뒤 设定 보충」을 추가했습니다. 매 묶음 细纲을 다 짠 뒤 재사용될 신규 유명 캐릭터/세력/핵심 设定을 스캔해, 자동으로 `设定/角色|势力|世界观` 파일 + `追踪/角色状态` 초기 항목을 만듭니다. 卷纲/细纲으로 재사용 여부를 판단해, 일회성 엑스트라는 파일을 만들지 않습니다; 이미 존재하면 细纲 증분으로 보충하고 덮어쓰지 않습니다; 细纲이 이미 확정한 정보만 채우고 플레이스홀더는 남겨두며, 지어내지 않습니다. 산출물 매핑표에 `设定/角色|势力` 행을 보강; 단장 흐름 step 11에「正文中 첫 등장하는 재사용 캐릭터」를 같은 규칙으로 파일 만들기를 보강했습니다.(Closes #123)
- **SessionStart 훅(hook) 중문화**: `detect-story-gaps.sh`와 `session-start.sh`의 작가 지향 출력을 중문으로 바꿨습니다(`[WARN]`/`[INFO]` 레벨 표시와 `/story-setup` 등 명령명은 유지). 비기술 중문 작가가 매 세션 시작마다 느끼는 이해 비용을 낮췄습니다.
- **dialogue-mastery 언어 차별화 표를 7차원으로 보강**: 원래 표는 5행밖에 없어, 같은 파일의 자가 검사 목록과 character-designer 에이전트(agent)가 쓴「7차원 차별화」와 모순되었습니다;「신분이 표현에 미치는 영향 / 진행이 태도에 미치는 영향」두 차원을 보강해, 4개 바이트 동기 사본(long-write / short-write / agent-references / story-review)을 함께 업데이트했습니다.
- **문서 보정**: `README_EN` 설치 명령에 `-g` 글로벌 파라미터 + 글로벌/로컬 설명을 보강했고, 단편 구조 블록을 실제 파일명으로 바로잡았습니다(`正文.md` / `小节大纲.md` / `작품 분석库/`, 존재하지 않는 `References/` 삭제). `README.md`와 일치; `CONTRIBUTING`에서 CI 설명을 실제 4개 가드 스크립트 + `node --check`로 바로잡았습니다.
- **story 라우팅(다중 책 전환)**: 신규「책 전환/목록 보기」의도와 다중 책 전환 흐름을 추가했습니다(`追踪/`、`设定/`가 포함된 책 디렉터리를 스캔해 `.active-book`에 씀).

### 버그 수정

- **Windows에서 `python3`이 Store 플레이스홀더 프로그램을 발동시켜 exit 49가 나는 문제 수정(#121)**: 진짜 원인은 Windows에서 `python3`이 Microsoft Store의 App Execution Alias 플레이스홀더 프로그램으로 해석되어, 비대화식 자식 프로세스(Claude Code의 Git Bash)에서 침묵하며 `exit 49`를 반환한 것이었으며, 중문 경로와는 무관했습니다. 모든 문서화된「크로스 플랫폼 글자 수 통계」`python3` 호출을 인터프리터 탐지로 바꿨습니다(`python3`→`python`→`py` 중 사용 가능한 것을 고름); `validate-story-commit.sh`의 `command -v python3` 가드를 실제 돌려보는 탐지로 바꿨습니다(플레이스홀더 프로그램은 `command -v`를 존재한다고 오판시킵니다).( #122를 대체)
- **에이전트(agent) 템플릿 열거 표류 수정**: `story-architect` 감정 호선을 emotional-arc-design에 맞추고(V形/倒V形/W形/점진/만족 지연/급반), 章首 훅(hook)을「시작 전략에 따라 유형 선택」으로 바꾸고, 잔여 신비 공식을 삭제했습니다; `character-designer` 대화 권력 모드를 压制/反转/心死로 바꿨습니다(dialogue-mastery와 일치).

### 공학

- **크로스 플랫폼 python 가드**: 신규 `scripts/check-python-invocation.sh`(`skills/` 안에서 벌거숭이 `python3` 호출을 금지하며, `-c`/`-m`/`<<`/스크립트 경로를 커버하고, 탐지 목록과 설명 문구는 면제)와 `scripts/test-charcount-portable.sh`(중문 경로 + 알려진 글자 수 단언을 구성하고, `--stub` 모드로 exit-49 짝퉁 `python3`를 집어넣어 Windows 장애를 재현하고 사용 가능한 인터프리터로 폴백하는 것을 단언)를 추가했습니다; `cross-platform.yml` 세 플랫폼에 연결하고, Windows에서는 Git Bash로 stub 테스트를 돌립니다.
- **CI 구문 가드**: `cross-platform.yml` static-check에 신규 `node --check`를 추가해, 모든 `*-scraper.js` + `cdp-utils.js` + `setup-cdp-chrome.js`를 커버했습니다(이전에는 커버가 0이라 구문 회귀(regression)가 바로 본선에 들어갈 수 있었습니다).
- **채집 스크립트 견고성(7개 scraper)**: `writeFileSync` 전에 `fs.mkdirSync(OUTDIR,{recursive})`를 보강해(`--outdir`가 존재하지 않는 디렉터리를 가리켜도 ENOENT로 데이터를 잃지 않음); 벌거숭이 `main()`은 통일적으로 try/catch + `process.exit(1)`로 감쌉니다; fanqie에는 추가로 per-category / per-channel try/catch를 보강해(단일 카테고리/단일 채널 실패가 전체 라운드를 중단시키지 않음).
- **check-shared-files 정밀도 향상**: `character-basics` / `character-design-methods` / `character-relations`가 이전에는 통째로 면제되어 표류해도 경보가 울리지 않았습니다; 이제 story-short-analyze那份(분석가 시점 header를 달아 의도적으로 분기한 것)만 제외하고, 나머지 사본은 여전히 바이트 일치를 강요해 writer↔writer 표류에 대한 가드를 복원했습니다.

### 설명

- 동명 공유 파일 변경은 모두 `check-shared-files.sh`에 따라 바이트 단위로 전체 사본에 동기화했습니다; 세 플랫폼 CI 가드는 모두 녹색 신호입니다.
- `story-deslop` rubric 강화는 여전히 브랜치 개발 중이며, 후속 버전에 남겨둡니다.

## v0.6.13

> write 스킬(skill) references 일치성 수정 + 추상 개념 실행 가능화(실제 웹소설 예제 보강 / 은어 비유 삭제) + 같은 스킬(skill) 내 중복 제거(포인터화) + 에이전트(agent) 템플릿 열거 표류 수정

### 개선

- **추상 개념 실행 가능화**: 두 write 스킬(skill)의 이론 reference에서「정의만 있고 따라 쓸 수 없는」메타 개념에 구체적인 웹소설 예제를 보강하거나 빈말을 삭제했습니다——plot-emotion-system 추출 계층에「추처리문(追妻文) 단계별 추상 + 껍데기 갈아끼우기」관통 예제를 보강; plot-frameworks 스토리 구성에「소염(萧炎) 슬랩스틱」예제 + 소설 사차원 자가 진단을 통속적인 네 가지 항목으로 바꾸고, 나선형 병합에 실행 가능한 정의를 보강; style-commercial-theory(이름 변경됨) 예술화/극단화/대보/관념 어긋남/뜨세권(套路) 다섯 가지 쓰기마다 예제를 보강; plot-core-methods信息团 / 래들 인간 vs 복선 / 업그레이드 삼차원 / 금핑거 승화에 판단 근거와 예제를 보강; emotional-arc-design에서「스토리 = 감정 × 세계」신비 공식을 삭제하고 삼계층 감정 예제로 변경; outline-structure-theory幕 선택 근거를 비극 장르 용어에서 장르별로 바꾸고, 뿌리/줄기/가지 비유 열과 팔줄선 placeholder를 삭제; style-craft에서 사의/신미 심미 은어를 삭제; short genre-*에 기조 자가 진단/연애 마찰/장르 교차 융합 예제를 보강하고 수선 삼경계 등 구호를 삭제했습니다.
- **일치성 수정**: 단편 반전 정보差 임계값을 writing-workflow 3단계로 통일(villain-and-reveal은 포인터로 변경); 대화 비율을 45-65%로 통일(genre-writing-techniques 두 군데); workflow-revision Step3 번호 수정; SKILL 횡단 표 anti-ai-writing 괄호 주석을 실제 소절명으로 바꾸고「대화」행을 보강; long SKILL 두 군데 앵커명을 正文과 일치시켰습니다.
- **같은 스킬(skill) 내 중복 제거(포인터화)**: 권력 협상 대화(writing-craft→dialogue-mastery)、캐릭터 상태 템플릿(artifact-protocols→state-tracking)、五幕式(plot-frameworks→outline-structure-theory)、진영 손패법(plot-frameworks→plot-special-topics)을 각기 단일 진실 소스 + 같은 스킬(skill) 내 포인터로 정하고, 중복 블록을 삭제해(순감 약 130행) 스킬(skill) 간 참조는 하지 않았습니다.
- **명칭 오해 없애기**: `style-commercial-theory.md` → `commercial-core-methods.md`(문풍이 아니라 판매점/상업 전략을 전문으로 다룸); `format-and-structure.md` 제목「단편 포맷 규범」→「正文 포맷과 소절 구조」(전 장르 공통 레이아웃 하드 규칙을 담으며, 4개 사본 동기화).
- **F1 맵 계층화**: plot-core-methods에서「초급 마을 네 세력(전체 프레임) vs 맵 교체 세 세력(간이판)」이 계층화된 것이지 모순이 아님을 명확히 하고, 맵을 바꿀 때 현금화/자원 폐루프 경로를 잃지 않도록 안내했습니다(3개 사본 동기화).
- **opening-design 단편 어댑트**: short SKILL 라우팅处에「前3장」을 시작 첫 소절~前1/3로 읽고, 칠보법은 목표 글자 수에 따라 등비 축소로 사용한다고 적었습니다(바이트 고정된 opening-design 본체는 건드리지 않음).

### 버그 수정

- **에이전트(agent) 템플릿 열거 표류**: story-architect 오도 기교「情感引导」→「情绪引导」、반전 유형 5→7로 늘려「인지/반전 없음」을 보강했습니다(reversal-toolkit 및 작품 분석 `_meta.json.reversal_type` 계약과 일치); character-designer 관계 명명「结盟型/权力型」→「联盟型/权威型」으로 바꿨습니다(character-relations와 일치).

### 설명

- 동명 공유 파일 변경은 모두 `check-shared-files.sh`에 따라 바이트 단위로 전체 사본에 동기화했습니다; 삼중 가드(check-shared-files / static-check / check-story-setup-deployment)는 모두 녹색 신호입니다.
- 보류 항목(후속으로 별도 방향 정해야 함): `check-shared-files.sh` IGNORE 논리 세분화(character-*가 write 측에서는 이미 바이트가 같으면서도 통째로 면제되어 있으므로,「스킬(skill) 대」별 면제로 바꿔야 하며 분류 목록은 이미 준비됨)、에이전트(agent) 템플릿 소수 canonical-conflict 열거(章首 훅(hook)7식 / 감정 호선6종 / 언어 스타일5vs7차원 / 대화 권력 모드)、에이전트(agent) 템플릿 얇은 색인 중복 제거.

## v0.6.12

> 주제 의사결정(처방): 순위 스캔→실행 가능성 판단→베스트셀러 원인 가설→작품 분석 되채우기 · references 주제별 색인 + 검색 검증 가능 · 여성 독자 대상 장편 플레이북 · 용어 백화(자조 비유 삭제) · 공학 가드(CI 검사 추가 + 채집 스크립트 견고성)

### 개선

- **story-long-scan(주제 의사결정)**: Phase 4를「대화 속에서 매치」에서 지속 가능한 `选题决策.md` 산출로 업그레이드했습니다.「주제 네 보」로 2-3개의 추천 주제를 냅니다(터질 수 있는 이유[작품 분석 검증 대기] / 시장 검증 / 차별화 포지셔닝 / 실행 가능성 높음·중·낮음 + 실패 리스크 + 검증 동작 / 분량 플랫폼). 실행 가능성은 기존 `[数据稀疏]`/<15 샘플 게이트로 상한을 걸었으며(샘플이 부족하면「높음」을 주지 않음), 내장 지식 모드는 일률적으로「중」입니다. 방법은 신규 `references/topic-decision.md`를 참고하세요.
- **story-long-analyze(베스트셀러 원인 되채우기)**: Stage 5 종합 리포트 산출 후, 프로젝트 루트에 `选题决策.md`가 있으면 장르 키워드로 매칭해 해당 주제의「터질 수 있는 이유」를 되채웁니다(이 책의 쓰기 기법/본받을 만한 뜨세권(套路)/핵심 메커니즘을 인용하며, 단일 책 가설 수준 지지로 표기). 다중 매치 시 사용자에게 묻고, 매치 없으면 침묵하며 건너뛰며, 이미 채워진 곳은 덮어쓰지 않습니다. Stage 5 최종 상태에 앵커를 박아, Stage 6(문풍, 실패 허용)의 영향을 받지 않습니다.
- **story-long-write(주제 소비)**: Phase 1에서 먼저 프로젝트 루트 `选题决策.md`를 찾습니다——존재하면 실행 가능성이 가장 높은 주제를 책 시작 기점으로 삼고 순위 스캔 날짜를 보고 데이터 신선도를 확인합니다; 누락되면 경로를 안내한 뒤 기존 주제 질문으로 폴백합니다.
- **story-long-write / story-short-write(주제별 색인)**: 두 write SKILL.md에「주제별 빠른 위치 찾기」횡단 주제 색인을 신규 추가했습니다(쾌감 포인트/감정/리듬/클라이맥스/금핑거/감정선/반전/캐릭터/AI스러움 제거). 각 주제마다 권위 파일 + 연계 파일을 하나씩 제시하고; 쾌감 포인트는「설계/역전/슬랩스틱/장르 공식」의도에 따라 분기합니다. 검색 향상은 A/B 실측(색인 있는 쪽 vs 없는 쪽)으로 입증했습니다.
- **story-long-write(여성 독자 대상 장편)**: 신규 `references/female-audience-writing.md`——여성 독자 핵심 원칙, 카피 구조, 장기 장르 뼈대, 卷级 감정 리듬, 다중 플랫폼(판치에(番茄小说) 여성/치디엔(起点中文网) 여성/진장(晋江)/치마오(七猫)) 쓰기 포지셔닝을 다룹니다.
- **흐름 이음새 보강**: story-setup、story-review에「흐름 이음새」절을 보강했습니다(표지/브라우저 도구 등 변두리 스킬(skill)에는 강요하지 않음); story `주제 의사결정` 라우팅 → story-long-scan.
- **story-short-write**: `output-contract.md`에 Phase 2「대상/벤치마크 컨텍스트 로딩」+ 참고